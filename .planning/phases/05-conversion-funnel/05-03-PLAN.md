---
phase: 05-conversion-funnel
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/islands/FundingQualifierForm.tsx
  - src/pages/foerderung.astro
  - src/pages/en/funding.astro
autonomous: true
requirements:
  - FUND-01
  - FUND-02
  - FUND-03
  - FUND-04
  - FORMS-02
  - FORMS-03
  - FORMS-04
  - FORMS-05
  - FORMS-06
  - FORMS-07

must_haves:
  truths:
    - "DE /foerderung/ and EN /en/funding/ both build and render the de-minimis content structure (D-17): what-is → eligibility → how KONVOI qualifies → calculate savings link → apply form"
    - "Funding page cites BG catalog section 1.10 'Aufwendungen für Maßnahmen zur Vermeidung von Diebstählen' per FUND-02 / D-15"
    - "FundingQualifierForm Preact island has all D-14 fields: company name, company size (dropdown 4 options), fleet size, vertical, contact name, email, phone (optional)"
    - "FundingQualifierForm validates with Zod: required company, size, fleetSize ≥ 1, vertical enum, contactName, email format; phone optional; dsgvoConsent must be true"
    - "FundingQualifierForm shows inline field-level errors; all filled values preserved on validation failure (FORMS-07)"
    - "FundingQualifierForm has hidden _gotcha honeypot and Cloudflare Turnstile explicit-render widget"
    - "FundingQualifierForm POSTs to Formspree with Accept: application/json; redirects to /danke or /en/thanks/ on success"
    - "Funding page cross-links to /roi (DE) or /en/roi (EN) for combined savings figure (FUND-04 / D-16)"
  artifacts:
    - path: "src/components/islands/FundingQualifierForm.tsx"
      provides: "Funding pre-qualification form Preact island"
      exports: ["default (FundingQualifierForm)"]
    - path: "src/pages/foerderung.astro"
      provides: "DE funding page at /foerderung/ with de-minimis content + form"
      contains: "foerderung"
    - path: "src/pages/en/funding.astro"
      provides: "EN funding page at /en/funding/ with de-minimis content + form"
      contains: "funding"
  key_links:
    - from: "src/pages/foerderung.astro"
      to: "src/components/islands/FundingQualifierForm.tsx"
      via: "<FundingQualifierForm locale={locale} client:visible />"
      pattern: "client:visible"
    - from: "src/pages/foerderung.astro"
      to: "/roi"
      via: "cross-link anchor per FUND-04"
      pattern: "roi"
    - from: "src/components/islands/FundingQualifierForm.tsx"
      to: "Formspree API"
      via: "fetch with Accept: application/json header"
      pattern: "application/json"
---

<objective>
Build the FundingQualifierForm Preact island and the DE+EN funding pages.

Purpose: The funding page (FUND-01..04) completes the bottom-of-funnel for prospects
who want to understand the 80% de-minimis subsidy before committing. It runs in parallel
with Plan 02 since it shares no files with Plan 02.

Output: FundingQualifierForm.tsx island and two Astro pages (foerderung, en/funding).
Plan 02 (ConsultForm) and Plan 03 (FundingQualifierForm) are fully parallel — no shared
files, both depend only on Plan 01's data layer.
</objective>

<execution_context>
@/c/Users/Rami/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/Rami/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-conversion-funnel/05-CONTEXT.md
@.planning/phases/05-conversion-funnel/05-RESEARCH.md

@src/data/brand/canonical.yaml
@src/data/pricing.ts
@src/i18n/translations.ts
@src/i18n/routeMap.ts
@src/components/islands/SensorDataViz.tsx
</context>

<interfaces>
<!-- Contracts from Plan 01 needed here. -->

From src/data/brand/canonical.yaml (funding section):
```yaml
funding:
  de_minimis_max_percent: 80
  de_catalog_ref: "Aufwendungen für Maßnahmen zur Vermeidung von Diebstählen"
  de_catalog_section: "1.10"
```

From src/i18n/translations.ts (keys added in Plan 01 Task 2):
- funding.hero_tagline, funding.hero_title, funding.hero_subtitle
- funding.section_what_title, funding.section_what_body
- funding.section_eligibility_title, funding.section_eligibility_body
- funding.section_konvoi_title, funding.section_konvoi_body
- funding.section_roi_link
- funding.qualifier_title, funding.qualifier_subtitle
- funding.catalog_ref
- form.* keys for field labels and error messages

From src/i18n/routeMap.ts:
```typescript
funding: { de: '/foerderung', en: '/en/funding' },
roi:     { de: '/roi',        en: '/en/roi' },
```

Zod import (in .tsx files — use 'zod' not 'astro/zod'):
```typescript
import { z } from 'zod';
```

Turnstile script tag must be on each Astro page that uses a form island:
```astro
<script is:inline src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit"></script>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build FundingQualifierForm Preact island (D-08, D-11, D-12, D-14, FUND-03, FORMS-02..07)</name>
  <files>src/components/islands/FundingQualifierForm.tsx</files>
  <action>
Create `src/components/islands/FundingQualifierForm.tsx`. This island is structurally
parallel to ConsultForm.tsx — same Formspree/Zod/Turnstile/DSGVO pattern, different fields.

Read `src/components/islands/ConsultForm.tsx` first (created in Plan 02) to reuse the
exact same Turnstile/honeypot/DSGVO/submit patterns. Do not reinvent.

**D-14 fields (FUND-03):**
- companyName (required, min 1)
- companySize (dropdown: 1_10, 11_50, 51_200, 201_plus — 4 options)
- fleetSize (number, coerce int, min 1)
- vertical (dropdown: same 4 options as ConsultForm)
- contactName (required, min 2)
- email (required, email format)
- phone (optional string — no format enforcement for v1)
- _gotcha (honeypot, must be empty)
- dsgvoConsent (boolean, must be true)

**No URL param pre-fill** — FundingQualifierForm is not linked from ROI calculator.

**Formspree endpoint:** `import.meta.env.PUBLIC_FORMSPREE_FUNDING_ID` (separate form from
ConsultForm per RESEARCH.md §Open Questions — different form ID for routing in Formspree).
Fallback: `'REPLACE_WITH_FORMSPREE_FUNDING_ID'`.

**Same Turnstile site key** as ConsultForm: `PUBLIC_TURNSTILE_SITE_KEY`.

**RESEARCH.md Pitfall 3 (values lost on error):** Use a single `formData` state object,
update on every input change. On validation error, only update `errors` state — do not
reset `formData`.

**RESEARCH.md Pitfall 7 (locale redirect):** Use `locale` prop for redirect decision.
```typescript
const thankYouUrl = locale === 'en' ? '/en/thanks/' : '/danke';
```

**Container ID for Turnstile:** Use `'turnstile-funding'` (different from ConsultForm's
`'turnstile-consult'`) so both islands can coexist on a page without ID collision.

```typescript
// src/components/islands/FundingQualifierForm.tsx
// Preact island — hydrated client:visible.
// Per D-08: FundingQualifierForm Preact island (FORMS-02).
// Per D-14: fields: companyName, companySize, fleetSize, vertical, contactName, email, phone (optional).
// Per D-09: client-side POST to Formspree via fetch, no page reload.
// Per D-10: Zod validation before POST; inline field errors (FORMS-03, FORMS-07).
// Per D-11: _gotcha honeypot + Cloudflare Turnstile explicit render (FORMS-04).
// Per D-12: DSGVO consent checkbox, unchecked, links to /datenschutz (FORMS-05).

import { useEffect, useRef, useState } from 'preact/hooks';
import { z } from 'zod';
import type { Vertical } from '~/data/pricing';

declare global {
  interface Window {
    turnstile?: {
      render: (container: string | HTMLElement, options: object) => string;
      remove: (widgetId: string) => void;
      reset: (widgetId: string) => void;
    };
  }
}

const FORMSPREE_ID =
  (import.meta.env.PUBLIC_FORMSPREE_FUNDING_ID as string | undefined) ??
  'REPLACE_WITH_FORMSPREE_FUNDING_ID';

const TURNSTILE_SITE_KEY =
  (import.meta.env.PUBLIC_TURNSTILE_SITE_KEY as string | undefined) ??
  '1x00000000000000000000AA'; // Cloudflare test key (always passes) for dev

const VERTICALS: { value: Vertical; de: string; en: string }[] = [
  { value: 'high-value',  de: 'Hochwertige Güter',  en: 'High-Value Cargo' },
  { value: 'cooling',     de: 'Kühltransport',       en: 'Refrigerated Transport' },
  { value: 'intermodal',  de: 'Intermodal',          en: 'Intermodal' },
  { value: 'other',       de: 'Sonstiges',           en: 'Other' },
];

const COMPANY_SIZES: { value: string; de: string; en: string }[] = [
  { value: '1_10',     de: '1–10 Mitarbeiter',   en: '1–10 employees' },
  { value: '11_50',    de: '11–50 Mitarbeiter',  en: '11–50 employees' },
  { value: '51_200',   de: '51–200 Mitarbeiter', en: '51–200 employees' },
  { value: '201_plus', de: '201+ Mitarbeiter',   en: '201+ employees' },
];

// Zod schema per D-10, D-14
const fundingFormSchema = z.object({
  companyName:  z.string().trim().min(1, 'required'),
  companySize:  z.enum(['1_10', '11_50', '51_200', '201_plus'], {
                  errorMap: () => ({ message: 'required' }),
                }),
  fleetSize:    z.coerce.number().int().min(1, 'min1'),
  vertical:     z.enum(['high-value', 'cooling', 'intermodal', 'other'], {
                  errorMap: () => ({ message: 'required' }),
                }),
  contactName:  z.string().trim().min(2, 'min2'),
  email:        z.string().email('email'),
  phone:        z.string().optional(),
  _gotcha:      z.string().refine((v) => v === '', { message: 'spam' }),
  dsgvoConsent: z.boolean().refine((v) => v === true, { message: 'required' }),
});

type FormData = {
  companyName: string;
  companySize: string;
  fleetSize: string;
  vertical: string;
  contactName: string;
  email: string;
  phone: string;
  _gotcha: string;
  dsgvoConsent: boolean;
};

interface Props {
  locale?: string;
}

export default function FundingQualifierForm({ locale = 'de' }: Props) {
  const isDE = locale !== 'en';

  const [formData, setFormData] = useState<FormData>({
    companyName: '', companySize: '1_10', fleetSize: '',
    vertical: 'high-value', contactName: '', email: '', phone: '',
    _gotcha: '', dsgvoConsent: false,
  });
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);

  const turnstileContainerRef = useRef<HTMLDivElement>(null);
  const turnstileWidgetId = useRef<string | undefined>(undefined);
  const captchaToken = useRef<string | null>(null);

  // Turnstile explicit render per D-11
  // RESEARCH.md Pitfall 2: check window.turnstile before calling render
  useEffect(() => {
    if (!turnstileContainerRef.current) return;
    const tryRender = () => {
      if (!window.turnstile) return;
      turnstileWidgetId.current = window.turnstile.render(
        turnstileContainerRef.current!,
        {
          sitekey: TURNSTILE_SITE_KEY,
          callback: (token: string) => { captchaToken.current = token; },
          'expired-callback': () => { captchaToken.current = null; },
        }
      );
    };
    const timer = setTimeout(tryRender, 100);
    return () => {
      clearTimeout(timer);
      if (turnstileWidgetId.current && window.turnstile) {
        window.turnstile.remove(turnstileWidgetId.current);
        turnstileWidgetId.current = undefined;
        captchaToken.current = null;
      }
    };
  }, []);

  const set = (field: keyof FormData, value: string | boolean) =>
    setFormData((prev) => ({ ...prev, [field]: value }));

  const errLabel = (code: string): string => {
    const map: Record<string, { de: string; en: string }> = {
      required: { de: 'Pflichtfeld.',                      en: 'Required field.' },
      min2:     { de: 'Mindestens 2 Zeichen.',             en: 'At least 2 characters.' },
      email:    { de: 'Gültige E-Mail erforderlich.',      en: 'Valid email required.' },
      min1:     { de: 'Mindestgröße: 1 Trailer.',          en: 'Minimum: 1 trailer.' },
      spam:     { de: 'Spam erkannt.',                     en: 'Spam detected.' },
      captcha:  { de: 'Bitte Sicherheitscheck abschließen.', en: 'Please complete the security check.' },
      submit:   { de: 'Fehler beim Senden. Bitte erneut versuchen.', en: 'Submission failed. Please try again.' },
    };
    return (isDE ? map[code]?.de : map[code]?.en) ?? code;
  };

  const handleSubmit = async (e: Event) => {
    e.preventDefault();

    // Step 1: Zod validation (D-10, FORMS-03)
    const result = fundingFormSchema.safeParse(formData);
    if (!result.success) {
      const fieldErrors: Record<string, string> = {};
      result.error.errors.forEach((err) => {
        const key = String(err.path[0] ?? '_form');
        fieldErrors[key] = err.message;
      });
      setErrors(fieldErrors);
      return; // PITFALL GUARD: formData untouched — values preserved (FORMS-07)
    }

    // Step 2: Require Turnstile token (D-11, FORMS-04)
    if (!captchaToken.current) {
      setErrors({ _form: 'captcha' });
      return;
    }

    setSubmitting(true);
    setErrors({});

    // Step 3: POST to Formspree (D-09)
    // CRITICAL: Accept: application/json is LOAD-BEARING (RESEARCH.md Pitfall 1)
    try {
      const payload = {
        ...result.data,
        _gotcha: undefined,
        'cf-turnstile-response': captchaToken.current,
      };
      const res = await fetch(`https://formspree.io/f/${FORMSPREE_ID}`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json', // LOAD-BEARING: without this, Formspree returns HTML redirect
        },
        body: JSON.stringify(payload),
      });
      if (!res.ok) throw new Error(`Formspree ${res.status}`);

      // Step 4: Redirect per D-09 / FORMS-06
      // RESEARCH.md Pitfall 7: use locale prop, not pathname sniffing
      const thankYouUrl = locale === 'en' ? '/en/thanks/' : '/danke';
      window.location.href = thankYouUrl;
    } catch {
      // FORMS-07: preserve formData, show inline error
      setErrors({ _form: 'submit' });
      setSubmitting(false);
      captchaToken.current = null;
      if (turnstileWidgetId.current && window.turnstile) {
        window.turnstile.reset(turnstileWidgetId.current);
      }
    }
  };

  const datenschutzHref = isDE ? '/datenschutz' : '/en/privacy/';

  return (
    <div class="rounded-2xl border border-slate-200 bg-white p-8 shadow-sm dark:border-slate-700 dark:bg-slate-900">
      <h2 class="mb-2 text-2xl font-bold text-slate-900 dark:text-white">
        {isDE ? 'Förder-Vorqualifizierung' : 'Funding Pre-Qualification'}
      </h2>
      <p class="mb-6 text-slate-500 dark:text-slate-400">
        {isDE
          ? 'Kostenlose Prüfung Ihrer BG-Förderberechtigung. Wir melden uns innerhalb von 24 Stunden.'
          : 'Free check of your BG subsidy eligibility. We will be in touch within 24 hours.'}
      </p>

      {errors._form && (
        <div role="alert" class="mb-4 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700 dark:border-red-800 dark:bg-red-950 dark:text-red-300">
          {errLabel(errors._form)}
        </div>
      )}

      <form onSubmit={handleSubmit} noValidate>
        {/* Honeypot — hidden per D-11, RESEARCH.md Pitfall 6 */}
        <input
          type="text"
          name="_gotcha"
          value={formData._gotcha}
          onInput={(e) => set('_gotcha', (e.target as HTMLInputElement).value)}
          style="display:none;position:absolute;left:-9999px"
          tabIndex={-1}
          autoComplete="off"
          aria-hidden="true"
        />

        <div class="grid gap-5">
          {/* Company name */}
          <div>
            <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-300">
              {isDE ? 'Unternehmen' : 'Company'} <span class="text-red-500">*</span>
            </label>
            <input
              type="text"
              name="companyName"
              value={formData.companyName}
              onInput={(e) => set('companyName', (e.target as HTMLInputElement).value)}
              aria-invalid={!!errors.companyName}
              class={`w-full rounded-lg border px-4 py-2.5 text-sm ${errors.companyName ? 'border-red-400 bg-red-50' : 'border-slate-300 bg-white'} dark:border-slate-600 dark:bg-slate-800`}
            />
            {errors.companyName && <p class="mt-1 text-xs text-red-600">{errLabel(errors.companyName)}</p>}
          </div>

          {/* Company size + Fleet size — two columns */}
          <div class="grid gap-5 sm:grid-cols-2">
            <div>
              <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-300">
                {isDE ? 'Unternehmensgröße' : 'Company Size'} <span class="text-red-500">*</span>
              </label>
              <select
                name="companySize"
                value={formData.companySize}
                onChange={(e) => set('companySize', (e.target as HTMLSelectElement).value)}
                aria-invalid={!!errors.companySize}
                class={`w-full rounded-lg border px-4 py-2.5 text-sm ${errors.companySize ? 'border-red-400 bg-red-50' : 'border-slate-300 bg-white'} dark:border-slate-600 dark:bg-slate-800`}
              >
                {COMPANY_SIZES.map((s) => (
                  <option key={s.value} value={s.value}>{isDE ? s.de : s.en}</option>
                ))}
              </select>
              {errors.companySize && <p class="mt-1 text-xs text-red-600">{errLabel(errors.companySize)}</p>}
            </div>
            <div>
              <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-300">
                {isDE ? 'Flottengröße (Trailer)' : 'Fleet Size (Trailers)'} <span class="text-red-500">*</span>
              </label>
              <input
                type="number"
                name="fleetSize"
                min="1"
                value={formData.fleetSize}
                onInput={(e) => set('fleetSize', (e.target as HTMLInputElement).value)}
                aria-invalid={!!errors.fleetSize}
                class={`w-full rounded-lg border px-4 py-2.5 text-sm ${errors.fleetSize ? 'border-red-400 bg-red-50' : 'border-slate-300 bg-white'} dark:border-slate-600 dark:bg-slate-800`}
              />
              {errors.fleetSize && <p class="mt-1 text-xs text-red-600">{errLabel(errors.fleetSize)}</p>}
            </div>
          </div>

          {/* Vertical */}
          <div>
            <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-300">
              {isDE ? 'Hauptbranche' : 'Primary Vertical'} <span class="text-red-500">*</span>
            </label>
            <select
              name="vertical"
              value={formData.vertical}
              onChange={(e) => set('vertical', (e.target as HTMLSelectElement).value)}
              aria-invalid={!!errors.vertical}
              class={`w-full rounded-lg border px-4 py-2.5 text-sm ${errors.vertical ? 'border-red-400 bg-red-50' : 'border-slate-300 bg-white'} dark:border-slate-600 dark:bg-slate-800`}
            >
              {VERTICALS.map((v) => (
                <option key={v.value} value={v.value}>{isDE ? v.de : v.en}</option>
              ))}
            </select>
            {errors.vertical && <p class="mt-1 text-xs text-red-600">{errLabel(errors.vertical)}</p>}
          </div>

          {/* Contact name + Email — two columns */}
          <div class="grid gap-5 sm:grid-cols-2">
            <div>
              <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-300">
                {isDE ? 'Ansprechpartner' : 'Contact Name'} <span class="text-red-500">*</span>
              </label>
              <input
                type="text"
                name="contactName"
                value={formData.contactName}
                onInput={(e) => set('contactName', (e.target as HTMLInputElement).value)}
                aria-invalid={!!errors.contactName}
                class={`w-full rounded-lg border px-4 py-2.5 text-sm ${errors.contactName ? 'border-red-400 bg-red-50' : 'border-slate-300 bg-white'} dark:border-slate-600 dark:bg-slate-800`}
              />
              {errors.contactName && <p class="mt-1 text-xs text-red-600">{errLabel(errors.contactName)}</p>}
            </div>
            <div>
              <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-300">
                {isDE ? 'E-Mail' : 'Email'} <span class="text-red-500">*</span>
              </label>
              <input
                type="email"
                name="email"
                value={formData.email}
                onInput={(e) => set('email', (e.target as HTMLInputElement).value)}
                aria-invalid={!!errors.email}
                class={`w-full rounded-lg border px-4 py-2.5 text-sm ${errors.email ? 'border-red-400 bg-red-50' : 'border-slate-300 bg-white'} dark:border-slate-600 dark:bg-slate-800`}
              />
              {errors.email && <p class="mt-1 text-xs text-red-600">{errLabel(errors.email)}</p>}
            </div>
          </div>

          {/* Phone (optional) */}
          <div>
            <label class="mb-1 block text-sm font-medium text-slate-700 dark:text-slate-300">
              {isDE ? 'Telefon (optional)' : 'Phone (optional)'}
            </label>
            <input
              type="tel"
              name="phone"
              value={formData.phone}
              onInput={(e) => set('phone', (e.target as HTMLInputElement).value)}
              class="w-full rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-sm dark:border-slate-600 dark:bg-slate-800"
            />
          </div>

          {/* Turnstile widget container (D-11) — different id from ConsultForm */}
          <div ref={turnstileContainerRef} id="turnstile-funding" />

          {/* DSGVO consent per D-12, FORMS-05 — unchecked by default */}
          <div>
            <label class="flex items-start gap-3 text-sm text-slate-600 dark:text-slate-300">
              <input
                type="checkbox"
                name="dsgvoConsent"
                checked={formData.dsgvoConsent}
                onChange={(e) => set('dsgvoConsent', (e.target as HTMLInputElement).checked)}
                aria-invalid={!!errors.dsgvoConsent}
                class="mt-0.5 h-4 w-4 rounded border-slate-300 accent-konvoi-primary"
              />
              <span>
                {isDE ? 'Ich habe die ' : 'I have read the '}
                <a href={datenschutzHref} class="underline text-konvoi-primary" target="_blank" rel="noopener">
                  {isDE ? 'Datenschutzerklärung' : 'Privacy Policy'}
                </a>
                {isDE ? ' gelesen und stimme zu.' : ' and agree.'}
                <span class="text-red-500 ml-1">*</span>
              </span>
            </label>
            {errors.dsgvoConsent && (
              <p class="mt-1 text-xs text-red-600">{errLabel(errors.dsgvoConsent)}</p>
            )}
          </div>

          {/* Submit */}
          <button
            type="submit"
            disabled={submitting}
            class="w-full rounded-lg bg-konvoi-primary px-6 py-3 font-semibold text-white transition hover:bg-konvoi-primary/90 disabled:opacity-60"
          >
            {submitting
              ? (isDE ? 'Wird gesendet…' : 'Sending…')
              : (isDE ? 'Vorqualifizierung anfragen' : 'Apply for pre-qualification')}
          </button>
        </div>
      </form>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && pnpm check:astro 2>&1 | tail -10</automated>
  </verify>
  <done>
    - src/components/islands/FundingQualifierForm.tsx exists
    - grep "Accept.*application/json" src/components/islands/FundingQualifierForm.tsx returns a match
    - grep "_gotcha" src/components/islands/FundingQualifierForm.tsx returns a match
    - grep "dsgvoConsent" src/components/islands/FundingQualifierForm.tsx returns a match
    - grep "companySize" src/components/islands/FundingQualifierForm.tsx returns a match
    - grep "phone" src/components/islands/FundingQualifierForm.tsx returns a match (optional field)
    - grep "turnstile-funding" src/components/islands/FundingQualifierForm.tsx returns a match (unique Turnstile ID)
    - pnpm check:astro exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Build DE + EN funding pages (D-15, D-16, D-17, FUND-01, FUND-02, FUND-04)</name>
  <files>src/pages/foerderung.astro, src/pages/en/funding.astro</files>
  <action>
Create `src/pages/foerderung.astro` and `src/pages/en/funding.astro`.

**Check an existing Phase 4 page first** (e.g., `src/pages/produkt.astro`) to confirm:
1. The exact PageLayout import path
2. How to inject scripts into the page head
3. The Tailwind colour token for CTA buttons

**Content structure per D-17 (FUND-01, FUND-02):**
1. Hero — tagline + title + subtitle (from translations.ts funding.hero_* keys)
2. What is de-minimis — explanation + catalog reference (funding.section_what_*)
3. Eligibility criteria (funding.section_eligibility_*)
4. How KONVOI qualifies (funding.section_konvoi_*)
5. Calculate your savings — link to /roi or /en/roi (FUND-04 / D-16)
6. Apply now — FundingQualifierForm island

**Catalog citation per FUND-02 / D-15:** The page MUST reference the catalog section.
Use `brand.funding.de_catalog_section` and `brand.funding.de_catalog_ref` from
`canonical.yaml` to render the citation — do not hardcode the string in the Astro file.

**ROI cross-link per FUND-04 / D-16:** Link to `/roi` (DE) or `/en/roi` (EN) using the
`routeMap.ts` values. The link text comes from `t('funding.section_roi_link', locale)`.

**Turnstile script tag** must be present on both pages (same pattern as pricing pages).

**`src/pages/foerderung.astro`:**
```astro
---
// src/pages/foerderung.astro — DE funding page
// Per D-15: /foerderung/ explains 80% de-minimis subsidy citing catalog section 1.10.
// Per D-16: embeds FundingQualifierForm island.
// Per D-17: content structure: what-is → eligibility → how KONVOI qualifies → ROI link → form.
// Per FUND-04 / D-16: cross-link to /roi for combined savings-plus-subsidy figure.

import PageLayout from '~/layouts/PageLayout.astro';
import FundingQualifierForm from '~/components/islands/FundingQualifierForm';
import { t } from '~/i18n/translations';
import brand from '~/data/brand/canonical.yaml';

const locale = 'de';
const metadata = {
  title: 'Förderung — KONVOI GmbH',
  description: `Bis zu ${brand.funding.de_minimis_max_percent}% Förderung durch die Berufsgenossenschaft. KONVOI qualifiziert sich für BG-Katalog Abschnitt ${brand.funding.de_catalog_section}.`,
};
---
<PageLayout metadata={metadata}>
  <!-- Turnstile script — explicit render required per D-11 / RESEARCH.md Pattern 2 -->
  <script
    is:inline
    src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit"
  ></script>

  <!-- Hero -->
  <section class="mx-auto max-w-4xl px-4 py-16 text-center">
    <p class="mb-3 text-sm font-semibold uppercase tracking-wider text-konvoi-primary">
      {t('funding.hero_tagline', locale)}
    </p>
    <h1 class="text-4xl font-bold font-heading text-slate-900 dark:text-white">
      {t('funding.hero_title', locale)}
    </h1>
    <p class="mt-4 text-lg text-slate-600 dark:text-slate-300">
      {t('funding.hero_subtitle', locale)}
    </p>
    <!-- Catalog badge — cites section 1.10 per FUND-02 -->
    <div class="mt-6 inline-flex items-center gap-2 rounded-full border border-blue-200 bg-blue-50 px-4 py-2 text-sm font-medium text-blue-700 dark:border-blue-800 dark:bg-blue-950 dark:text-blue-300">
      <span>BG-Katalog §{brand.funding.de_catalog_section}</span>
      <span class="text-blue-400">·</span>
      <span>{brand.funding.de_catalog_ref}</span>
    </div>
  </section>

  <!-- Content: D-17 structure -->
  <div class="mx-auto max-w-3xl px-4 pb-12 space-y-12">

    <!-- 1. What is de-minimis -->
    <section>
      <h2 class="text-2xl font-bold font-heading text-slate-900 dark:text-white mb-3">
        {t('funding.section_what_title', locale)}
      </h2>
      <p class="text-slate-600 dark:text-slate-300 leading-relaxed">
        {t('funding.section_what_body', locale)}
      </p>
    </section>

    <!-- 2. Eligibility -->
    <section>
      <h2 class="text-2xl font-bold font-heading text-slate-900 dark:text-white mb-3">
        {t('funding.section_eligibility_title', locale)}
      </h2>
      <p class="text-slate-600 dark:text-slate-300 leading-relaxed">
        {t('funding.section_eligibility_body', locale)}
      </p>
    </section>

    <!-- 3. How KONVOI qualifies -->
    <section>
      <h2 class="text-2xl font-bold font-heading text-slate-900 dark:text-white mb-3">
        {t('funding.section_konvoi_title', locale)}
      </h2>
      <p class="text-slate-600 dark:text-slate-300 leading-relaxed">
        {t('funding.section_konvoi_body', locale)}
      </p>
    </section>

    <!-- 4. Cross-link to ROI calculator (FUND-04 / D-16) -->
    <section class="rounded-2xl border border-konvoi-primary/20 bg-konvoi-primary/5 p-6">
      <p class="text-slate-700 dark:text-slate-200 mb-3">
        {locale === 'de'
          ? `Berechnen Sie Ihre kombinierten Einsparungen — KONVOI-Einsparung plus ${brand.funding.de_minimis_max_percent}% De-minimis-Rückerstattung.`
          : `Calculate your combined savings — KONVOI savings plus ${brand.funding.de_minimis_max_percent}% de-minimis reimbursement.`}
      </p>
      <a
        href="/roi"
        class="inline-flex items-center gap-1 font-semibold text-konvoi-primary hover:underline"
      >
        {t('funding.section_roi_link', locale)} →
      </a>
    </section>

  </div>

  <!-- 5. FundingQualifierForm (D-16, FUND-03) -->
  <section id="qualify" class="mx-auto max-w-2xl px-4 py-16">
    <div class="mb-6 text-center">
      <h2 class="text-3xl font-bold font-heading text-slate-900 dark:text-white">
        {t('funding.qualifier_title', locale)}
      </h2>
      <p class="mt-2 text-slate-500 dark:text-slate-400">
        {t('funding.qualifier_subtitle', locale)}
      </p>
    </div>
    <!-- FundingQualifierForm built in Task 1. client:visible per D-08. -->
    <FundingQualifierForm locale={locale} client:visible />
  </section>
</PageLayout>
```

**`src/pages/en/funding.astro`** — exact mirror with `locale = 'en'`, EN metadata, and `/en/roi` cross-link:

```astro
---
import PageLayout from '~/layouts/PageLayout.astro';
import FundingQualifierForm from '~/components/islands/FundingQualifierForm';
import { t } from '~/i18n/translations';
import brand from '~/data/brand/canonical.yaml';

const locale = 'en';
const metadata = {
  title: 'Funding — KONVOI GmbH',
  description: `Up to ${brand.funding.de_minimis_max_percent}% subsidy via the employers' liability association. KONVOI qualifies under BG Catalogue Section ${brand.funding.de_catalog_section}.`,
};
---
<PageLayout metadata={metadata}>
  <script
    is:inline
    src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit"
  ></script>

  <section class="mx-auto max-w-4xl px-4 py-16 text-center">
    <p class="mb-3 text-sm font-semibold uppercase tracking-wider text-konvoi-primary">
      {t('funding.hero_tagline', locale)}
    </p>
    <h1 class="text-4xl font-bold font-heading text-slate-900 dark:text-white">
      {t('funding.hero_title', locale)}
    </h1>
    <p class="mt-4 text-lg text-slate-600 dark:text-slate-300">
      {t('funding.hero_subtitle', locale)}
    </p>
    <div class="mt-6 inline-flex items-center gap-2 rounded-full border border-blue-200 bg-blue-50 px-4 py-2 text-sm font-medium text-blue-700 dark:border-blue-800 dark:bg-blue-950 dark:text-blue-300">
      <span>BG Catalogue §{brand.funding.de_catalog_section}</span>
      <span class="text-blue-400">·</span>
      <span>{brand.funding.de_catalog_ref}</span>
    </div>
  </section>

  <div class="mx-auto max-w-3xl px-4 pb-12 space-y-12">
    <section>
      <h2 class="text-2xl font-bold font-heading text-slate-900 dark:text-white mb-3">{t('funding.section_what_title', locale)}</h2>
      <p class="text-slate-600 dark:text-slate-300 leading-relaxed">{t('funding.section_what_body', locale)}</p>
    </section>
    <section>
      <h2 class="text-2xl font-bold font-heading text-slate-900 dark:text-white mb-3">{t('funding.section_eligibility_title', locale)}</h2>
      <p class="text-slate-600 dark:text-slate-300 leading-relaxed">{t('funding.section_eligibility_body', locale)}</p>
    </section>
    <section>
      <h2 class="text-2xl font-bold font-heading text-slate-900 dark:text-white mb-3">{t('funding.section_konvoi_title', locale)}</h2>
      <p class="text-slate-600 dark:text-slate-300 leading-relaxed">{t('funding.section_konvoi_body', locale)}</p>
    </section>
    <!-- Cross-link to /en/roi per FUND-04 -->
    <section class="rounded-2xl border border-konvoi-primary/20 bg-konvoi-primary/5 p-6">
      <p class="text-slate-700 dark:text-slate-200 mb-3">
        Calculate your combined savings — KONVOI savings plus {brand.funding.de_minimis_max_percent}% de-minimis reimbursement.
      </p>
      <a href="/en/roi" class="inline-flex items-center gap-1 font-semibold text-konvoi-primary hover:underline">
        {t('funding.section_roi_link', locale)} →
      </a>
    </section>
  </div>

  <section id="qualify" class="mx-auto max-w-2xl px-4 py-16">
    <div class="mb-6 text-center">
      <h2 class="text-3xl font-bold font-heading text-slate-900 dark:text-white">{t('funding.qualifier_title', locale)}</h2>
      <p class="mt-2 text-slate-500 dark:text-slate-400">{t('funding.qualifier_subtitle', locale)}</p>
    </div>
    <FundingQualifierForm locale={locale} client:visible />
  </section>
</PageLayout>
```
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/pages/foerderung.astro and src/pages/en/funding.astro both exist
    - grep "catalog_section\|catalog_ref" src/pages/foerderung.astro returns matches (catalog citation from canonical.yaml)
    - grep "FundingQualifierForm" src/pages/foerderung.astro returns a match (island embedded)
    - grep "client:visible" src/pages/foerderung.astro returns a match
    - grep '"/roi"' src/pages/foerderung.astro returns a match (ROI cross-link)
    - grep '"/en/roi"' src/pages/en/funding.astro returns a match (EN ROI cross-link)
    - pnpm build exits 0
    - dist/foerderung/index.html and dist/en/funding/index.html exist after build
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Formspree API | User-submitted funding qualification data crosses to external service |
| Browser → Cloudflare Turnstile CDN | Security widget loaded from Cloudflare CDN |
| canonical.yaml → Astro page | Catalog section/ref injected into static HTML at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | FundingQualifierForm — bot spam | mitigate | Cloudflare Turnstile explicit render + `_gotcha` honeypot; same defence-in-depth pattern as ConsultForm; Formspree rate-limits on backend |
| T-05-03-02 | Tampering | FundingQualifierForm — injection via form fields | mitigate | Zod schema validates all fields before POST; `z.string().trim()` on required fields; JSON.stringify encoding prevents injection in request body |
| T-05-03-03 | Tampering | Turnstile ID collision (`turnstile-funding` vs `turnstile-consult`) | mitigate | Container element IDs are distinct per form; `useRef` targets the specific container element; no DOM ID conflict possible when both forms are rendered on the same page |
| T-05-03-04 | Information Disclosure | PII in FundingQualifierForm state (companyName, contactName, email) | accept | Form state stored in Preact component memory only — ephemeral; no localStorage/sessionStorage; DSGVO consent required before submission per D-12 |
| T-05-03-05 | Spoofing | Funding page catalog citation (section 1.10) misrepresenting legal status | accept | Citation sourced from canonical.yaml which is manually curated; page copy is informational (not a legal guarantee); user reviews copy per D-07 "reasonable estimates" discretion; legal review recommended before launch |
</threat_model>

<verification>
After both tasks:
1. `test -f src/components/islands/FundingQualifierForm.tsx` passes
2. `grep "Accept.*application/json" src/components/islands/FundingQualifierForm.tsx` returns a match
3. `grep "_gotcha" src/components/islands/FundingQualifierForm.tsx` returns a match
4. `grep "dsgvoConsent" src/components/islands/FundingQualifierForm.tsx` returns a match
5. `grep "companySize" src/components/islands/FundingQualifierForm.tsx` returns a match
6. `grep "turnstile-funding" src/components/islands/FundingQualifierForm.tsx` returns a match
7. `test -f src/pages/foerderung.astro && test -f src/pages/en/funding.astro` passes
8. `grep "catalog_section" src/pages/foerderung.astro` returns a match (catalog citation)
9. `grep '"/roi"' src/pages/foerderung.astro` returns a match (ROI cross-link)
10. `pnpm build` exits 0
11. `test -f dist/foerderung/index.html && test -f dist/en/funding/index.html` passes
</verification>

<success_criteria>
- FundingQualifierForm.tsx: 7 fields (companyName/companySize/fleetSize/vertical/contactName/email/phone); Zod validation; values preserved on error; _gotcha honeypot; Turnstile with 'turnstile-funding' container ID; DSGVO checkbox; Formspree POST with Accept: application/json; locale-aware thank-you redirect
- foerderung.astro + en/funding.astro: hero with catalog badge citing canonical.yaml section 1.10; 4 content sections (what/eligibility/KONVOI/ROI-link); FundingQualifierForm island; Turnstile script tag; ROI cross-link to /roi and /en/roi respectively
- pnpm build exits 0; both pages in dist/
</success_criteria>

<output>
After completion, create `.planning/phases/05-conversion-funnel/05-03-SUMMARY.md` following the summary template.
</output>
