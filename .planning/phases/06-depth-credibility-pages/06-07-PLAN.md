---
phase: 06-depth-credibility-pages
plan: 07
type: execute
wave: 3
depends_on:
  - 06-06
files_modified: []
autonomous: false
requirements:
  - CASE-01
  - CASE-02
  - CASE-03
  - CASE-04
  - BLOG-01
  - BLOG-02
  - BLOG-03
  - BLOG-04
  - TEAM-01
  - TEAM-02
  - TEAM-03
  - CAREER-01
  - CAREER-02
  - CAREER-03
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "All 18 Phase 6 requirements verified as working in the browser"
    - "pnpm build passes with zero errors"
    - "No old [...blog] artifacts in dist/"
    - "Google Maps iframe absent from initial HTML source of /kontakt/"
    - "All 5 page sections reachable from site navigation"
  artifacts:
    - path: ".planning/phases/06-depth-credibility-pages/06-VERIFICATION.md"
      provides: "Phase 6 verification report"
  key_links: []
---

<objective>
Run the full Phase 6 build gate, perform automated checks, and pause for human visual
verification of all five new page sections before marking the phase complete.

Purpose: Confirm every requirement is met before Phase 7 begins.
Output: Verified build + VERIFICATION.md report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-depth-credibility-pages/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build gate + automated checks</name>
  <read_first>
    - .planning/phases/06-depth-credibility-pages/06-VALIDATION.md (per-task verification map)
  </read_first>
  <files></files>
  <action>
Run the following checks in sequence. Record pass/fail for each.

**Check 1 — Full build:**
```bash
pnpm build
```
Expected: exits 0, no TypeScript errors, no duplicate route warnings.

**Check 2 — No old blog routes in dist:**
```bash
ls dist/\[...blog\] 2>/dev/null && echo "FAIL: old blog dir found" || echo "PASS: no old blog dir"
```

**Check 3 — Case study routes exist in dist:**
```bash
ls dist/fallstudien/index.html && \
ls dist/fallstudien/schumacher/index.html && \
ls dist/fallstudien/jjx/index.html && \
ls dist/fallstudien/greilmeier/index.html && \
ls dist/en/case-studies/index.html && \
ls dist/en/case-studies/schumacher/index.html && \
echo "PASS: case study routes"
```

**Check 4 — Blog routes exist in dist:**
```bash
ls dist/aktuelles/index.html && \
ls dist/aktuelles/rss.xml && \
ls dist/en/news/index.html && \
ls dist/en/news/rss.xml && \
echo "PASS: blog routes"
```

**Check 5 — Team routes exist in dist:**
```bash
ls dist/team/index.html && \
ls dist/en/team/index.html && \
echo "PASS: team routes"
```

**Check 6 — Careers routes exist in dist:**
```bash
ls dist/karriere/index.html && \
ls dist/en/careers/index.html && \
echo "PASS: careers routes"
```

**Check 7 — Contact routes exist in dist:**
```bash
ls dist/kontakt/index.html && \
ls dist/en/contact/index.html && \
echo "PASS: contact routes"
```

**Check 8 — CONT-02 Google Maps iframe pre-consent check (CRITICAL security gate):**
```bash
grep -c "<iframe" dist/kontakt/index.html | xargs -I{} sh -c 'if [ "{}" -gt 0 ]; then grep "<iframe" dist/kontakt/index.html | grep " src=" | grep -v "data-src" && echo "FAIL: iframe has src= (DSGVO violation)" || echo "PASS: iframe has no src= (only data-src)"; else echo "PASS: no iframe in initial HTML"; fi'
```
Expected result: no `<iframe` with a `src=` attribute containing the Maps URL in the initial HTML.

**Check 9 — RSS feed is valid XML:**
```bash
xmllint --noout dist/aktuelles/rss.xml 2>&1 && echo "PASS: DE RSS valid XML" || echo "FAIL: DE RSS invalid XML"
xmllint --noout dist/en/news/rss.xml 2>&1 && echo "PASS: EN RSS valid XML" || echo "FAIL: EN RSS invalid XML"
```
If xmllint not available: `python3 -c "import xml.etree.ElementTree as ET; ET.parse('dist/aktuelles/rss.xml'); print('PASS')"` as fallback.

**Check 10 — Content counts:**
```bash
echo "Case studies DE: $(find src/content/caseStudy/de -name '*.md' | wc -l) (expect 3)"
echo "Case studies EN: $(find src/content/caseStudy/en -name '*.md' | wc -l) (expect 3)"
echo "Blog posts DE: $(find src/content/post/de -name '*.md' | wc -l) (expect 4+)"
echo "Team entries: $(find src/content/team -name '*.md' | wc -l) (expect 9)"
echo "Job entries DE: $(find src/content/job/de -name '*.md' | wc -l) (expect 8)"
echo "Event entries: $(find src/content/event -name '*.md' | wc -l) (expect 6)"
```

**Check 11 — Translation parity (existing CI check):**
```bash
pnpm run check 2>&1 | tail -20
```
Expected: exits 0 (Astro check + ESLint + Prettier all pass).

**Check 12 — Navigation wiring:**
```bash
grep "href: '/fallstudien/'" src/navigation.ts && \
grep "href: '/kontakt/'" src/navigation.ts && \
grep "href: '/en/case-studies/'" src/navigation.ts && \
grep "team:" src/i18n/routeMap.ts && \
echo "PASS: navigation wired"
```

Collect all results. If any check FAILS, fix the issue before proceeding to the human
verification checkpoint. Do not proceed to Task 2 with failing automated checks.
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `dist/fallstudien/schumacher/index.html` exists
    - `dist/aktuelles/rss.xml` exists
    - `dist/en/news/rss.xml` exists
    - `dist/team/index.html` exists
    - `dist/karriere/index.html` exists
    - `dist/kontakt/index.html` exists
    - `grep -c "src=" dist/kontakt/index.html | grep -v "data-src"` returns 0 matches for iframe src (Maps not pre-loaded)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <done>All automated checks pass. Build is green. No DSGVO violations detected in contact page HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of all Phase 6 pages</name>
  <what-built>
Five new page sections — case studies, blog, team, careers, and contact — built across
Plans 01–05 and wired into navigation in Plan 06. Total: ~50 new/modified files.
  </what-built>
  <how-to-verify>
Run `pnpm dev` and verify each section below. Work through the checklist in order.

**1. Case Studies**
- Visit http://localhost:4321/fallstudien/
  - [ ] 3 cards visible: Schumacher Group, JJX Logistics, Greilmeier Spedition
  - [ ] Each card shows customer name, vertical badge (e.g. "Hochwertige Guter"), outcome snippet
  - [ ] Hover on card shows shadow lift effect
  - [ ] Click a card to open detail page

- Visit http://localhost:4321/fallstudien/schumacher/
  - [ ] Quote in hero: "KONVOI gibt uns die Gewissheit..."
  - [ ] Three sections visible: Das Problem, Unser Ansatz, Das Ergebnis
  - [ ] CallToAction at bottom: "Beratung anfragen"

- Visit http://localhost:4321/en/case-studies/
  - [ ] Same 3 cards in English (customer names, EN vertical badges, EN outcome text)

- Visit http://localhost:4321/en/case-studies/schumacher/
  - [ ] Quote in English: "KONVOI gives us the certainty..."
  - [ ] Sections: "The Problem", "Our Approach", "The Outcome"

**2. Blog**
- Visit http://localhost:4321/aktuelles/
  - [ ] Page renders with "Aktuelles" heading
  - [ ] At least 4 blog post cards visible (migrated Jimdo posts)
  - [ ] RSS feed link visible

- Visit http://localhost:4321/aktuelles/rss.xml
  - [ ] Valid XML with feed items (title, link, pubDate per item)

- Visit http://localhost:4321/en/news/
  - [ ] Page renders (may show 0 posts — this is correct for v1 EN)
  - [ ] Empty state message visible if 0 posts

- Visit http://localhost:4321/en/news/rss.xml
  - [ ] Valid XML (may have 0 items — correct for v1)

**3. Team**
- Visit http://localhost:4321/team/
  - [ ] 9 team cards visible in responsive grid (4 columns on desktop)
  - [ ] Each card shows initials placeholder (or photo if available), name, title, bio
  - [ ] Alexander appears first (order:1), Justus appears last (order:9)
  - [ ] CallToAction at bottom

- Visit http://localhost:4321/en/team/
  - [ ] Same 9 members with English titles and bios

**4. Careers**
- Visit http://localhost:4321/karriere/
  - [ ] 8 role cards visible
  - [ ] Each card has department badge (e.g. "Vertrieb") and type badge (e.g. "Vollzeit")
  - [ ] "Bewerben" button visible on each card (min-height 44px)
  - [ ] Click "Bewerben" on any role — email client opens with:
    - To: applications@konvoi.eu
    - Subject: [role title] - Bewerbung

- Visit http://localhost:4321/en/careers/
  - [ ] Explicit message: "Our job listings are currently available in German only."
  - [ ] Link to /karriere/ visible and clickable
  - [ ] No auto-redirect occurs

**5. Contact**
- Visit http://localhost:4321/kontakt/
  - [ ] Two contact cards side by side (stacked on mobile)
  - [ ] Justus card: name, "Kundenberatung", phone +49 157 51959636 (clickable tel: link), email
  - [ ] Heinz card: name, "Investoren, Marketing & Bewerber", phone +49 163 9067060, email
  - [ ] Static placeholder visible where map would be (no Google Maps loaded yet)
  - [ ] "Karte laden" button visible
  - [ ] Click "Karte laden" — Google Maps iframe appears (map loads)
  - [ ] After loading: button text changes to "Karte ausblenden" or map is visible
  - [ ] Upcoming events list shows future events (all 6 should be visible since all dates are in future)
  - [ ] CallToAction at bottom: "Beratung anfragen"

  SECURITY CHECK — Right-click "View Page Source" on /kontakt/:
  - [ ] Search for "maps.google.com" or "maps/embed" — must NOT appear in initial HTML source
  - [ ] Only "data-src" attribute should reference the Maps URL (not src=)

- Visit http://localhost:4321/en/contact/
  - [ ] Same layout with English copy
  - [ ] "Load map" button (not "Karte laden")
  - [ ] Upcoming Events heading in English

**6. Navigation**
- On any page, check the header nav:
  - [ ] "Fallstudien" links to /fallstudien/ (not #)
  - [ ] "Uber uns" or "About" dropdown shows Team and Karriere/Careers sub-items
  - [ ] "Kontakt" links to /kontakt/ (not #)
  - [ ] Language switcher on /fallstudien/ links to /en/case-studies/
  - [ ] Language switcher on /team/ links to /en/team/
  </how-to-verify>
  <resume-signal>
Type "approved" if all checkboxes pass, or describe any issues found (e.g. "Maps iframe has src= in source, Bewerben mailto missing subject").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → dist/ | All generated HTML verified before phase close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | CONT-02 verification — iframe pre-consent | mitigate | Check 8 in Task 1 explicitly greps dist/kontakt/index.html for iframe src= attribute. Human verifier also checks page source in Task 2. Both gates must pass before phase is marked complete. |
</threat_model>

<verification>
Phase 6 is complete when:
1. All automated checks in Task 1 pass (pnpm build green, all dist/ paths exist, no DSGVO iframe violation)
2. Human verifier types "approved" in Task 2
3. VERIFICATION.md written to .planning/phases/06-depth-credibility-pages/
</verification>

<success_criteria>
- pnpm build exits 0 with no errors
- All 5 page sections visible and navigable in browser
- CONT-02 security check passes: no Maps iframe src= in initial HTML
- CAREER-02 check passes: Bewerben mailto has correct prefilled subject
- Language switcher works across all Phase 6 page pairs
- Human verifier confirms all checklist items pass
</success_criteria>

<output>
After human approval, create `.planning/phases/06-depth-credibility-pages/06-VERIFICATION.md`
with:
- Date of verification
- All 12 automated check results (pass/fail)
- Human verifier sign-off
- Any issues found and fixed during verification

Then create `.planning/phases/06-depth-credibility-pages/06-07-SUMMARY.md`
</output>
