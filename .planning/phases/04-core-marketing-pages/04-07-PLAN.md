---
phase: 04-core-marketing-pages
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/industry/de/hochwertige-gueter.md
  - src/content/industry/de/kuehlgut.md
  - src/content/industry/de/intermodal.md
  - src/content/industry/de/sonstige.md
  - src/content/industry/en/high-value.md
  - src/content/industry/en/cooling.md
  - src/content/industry/en/intermodal.md
  - src/content/industry/en/other.md
autonomous: true
requirements:
  - VERT-01
  - VERT-02
  - VERT-03
  - VERT-04

must_haves:
  truths:
    - "4 DE industry entries exist in src/content/industry/de/ with slug, riskProfile, relatedUseCases fields"
    - "4 EN industry entries exist in src/content/industry/en/ with matching translationKeys"
    - "pnpm check passes (Zod schema validation on all industry collection entries)"
    - "pnpm run check:translations passes (DE/EN parity for industry collection)"
  artifacts:
    - path: "src/content/industry/de/hochwertige-gueter.md"
      provides: "DE high-value industry vertical"
      contains: "riskProfile:"
    - path: "src/content/industry/en/high-value.md"
      provides: "EN high-value industry vertical"
      contains: "locale: en"
  key_links:
    - from: "src/content/industry/de/hochwertige-gueter.md"
      to: "src/pages/branchen/[slug].astro"
      via: "slug field matches route param"
      pattern: "slug: hochwertige-gueter"
    - from: "src/content/industry/*/high-value.md"
      to: "src/content/industry/*/hochwertige-gueter.md"
      via: "translationKey: high-value (shared across locales)"
      pattern: "translationKey: high-value"
---

<objective>
Create the 8 industry content collection markdown files for Phase 4: 4 DE + 4 EN entries.
These files are the single source of truth for all industry vertical page content — the
dynamic route pages in Plan 06 consume them.

Purpose: Separated from Plan 04 (useCase content) to keep both plans within the ~50% context
budget. Industry files have their own schema, risk profile copy, and cross-link structure.

Output: 8 markdown files with YAML frontmatter (Zod-validated) + 200-300 word DE/EN body
copy per industry vertical.
</objective>

<execution_context>
@/c/Users/Rami/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/Rami/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-core-marketing-pages/04-CONTEXT.md
@.planning/current-site-overview.md
@src/data/brand/voice.md
@src/data/brand/canonical.yaml
@src/content.config.ts
@src/i18n/routeMap.ts

<interfaces>
<!-- Schema contracts the executor must satisfy exactly. -->

industry collection schema (from src/content.config.ts):
```typescript
z.object({
  locale: z.enum(['de', 'en']),           // REQUIRED
  translationKey: z.string(),              // REQUIRED — same value for DE+EN pair
  canonicalKey: z.string(),               // REQUIRED — unique per entry
  title: z.string(),                      // REQUIRED
  slug: z.string(),                       // REQUIRED — must match routeMap.ts path segment
  riskProfile: z.string().optional(),
  relatedUseCases: z.array(z.string()).optional(), // DE slugs: ladungsdiebstahl, dieseldiebstahl, etc.
  publishDate: z.date().optional(),
  metadata: metadataDefinition(),
})
```

Industry slug mapping:
DE slug              | EN slug        | translationKey
hochwertige-gueter   | high-value     | high-value
kuehlgut             | cooling        | cooling
intermodal           | intermodal     | intermodal
sonstige             | other          | other

relatedUseCases values (used in industry entries): DE slugs only
  e.g. ['ladungsdiebstahl', 'fahrerangriffe', 'transparenz-der-operationen']

NOTE on generateId: The industryCollection uses a custom generateId function
(`${locale}/${slug}`). This prevents collision between DE intermodal (de/intermodal)
and EN intermodal (en/intermodal). Ensure each entry has distinct locale + slug combination.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 industry markdown files (4 DE + 4 EN)</name>
  <read_first>
    - src/content.config.ts (industryCollection schema — note generateId uses locale+slug)
    - src/i18n/routeMap.ts (exact DE and EN slug values for all 4 verticals)
    - src/data/brand/voice.md (preventive positioning for industry-specific copy)
    - .planning/current-site-overview.md (section 3: target market, section 7: vertical landing descriptions)
    - .planning/phases/04-core-marketing-pages/04-CONTEXT.md (D-15, D-16, D-17, D-18, D-19)
  </read_first>
  <files>
    src/content/industry/de/hochwertige-gueter.md,
    src/content/industry/de/kuehlgut.md,
    src/content/industry/de/intermodal.md,
    src/content/industry/de/sonstige.md,
    src/content/industry/en/high-value.md,
    src/content/industry/en/cooling.md,
    src/content/industry/en/intermodal.md,
    src/content/industry/en/other.md
  </files>
  <action>
Create all 8 industry markdown files. Each file follows this template
(per D-15 — 4 verticals, per D-16 — unique risk profile per vertical):

```yaml
# ---
locale: de
translationKey: high-value
canonicalKey: high-value-de
title: Hochwertige Güter
slug: hochwertige-gueter        # exact match to routeMap.ts DE path segment
riskProfile: |
  [2-3 sentence unique risk profile for this vertical]
relatedUseCases:
  - ladungsdiebstahl             # DE slugs from routeMap.ts
  - fahrerangriffe
  - transparenz-der-operationen
publishDate: 2026-04-24
# ---

[200-300 word body in markdown]
```

COPY RULES (voice.md — strictly enforced per D-19): Same preventive framing. Each vertical has
a unique risk profile that makes KONVOI relevant. Body structure: (1) Vertical overview →
(2) Unique risk profile → (3) KONVOI's fit for this vertical → (4) Relevant use cases (linked).
No exclamation marks. "KONVOI" always ALL CAPS.

**1. src/content/industry/de/hochwertige-gueter.md** (per D-15, D-16)
- translationKey: high-value
- canonicalKey: high-value-de
- slug: hochwertige-gueter
- title: Hochwertige Güter
- riskProfile: "Pharma, Elektronik, Edelmetalle und Kosmetik haben einen Warenwert, der einzelne Trailer zu Zielobjekten für organisierte Diebstahlringe macht. Ein einziger gestohlener Trailer kann Schäden von 500.000 bis 1,2 Millionen Euro verursachen."
- relatedUseCases: [ladungsdiebstahl, fahrerangriffe, transparenz-der-operationen]  (per D-17)
- Body: Industry description, TAPA 2024 cargo theft stats, pharmaceutical cold chain, organized crime targeting patterns, why KONVOI's preventive approach fits premium cargo, cross-links to 3 use cases.

**2. src/content/industry/de/kuehlgut.md** (per D-15, D-16)
- translationKey: cooling
- canonicalKey: cooling-de
- slug: kuehlgut
- title: Kühltransporte
- riskProfile: "Kühltrailer sind doppelter Bedrohung ausgesetzt: neben Ladungsdiebstahl gefährdet jede Betriebsunterbrechung die Kühlkette — mit Folgekosten durch Produktverfall und Haftungsansprüchen bei Pharma- oder Lebensmittellieferungen."
- relatedUseCases: [dieseldiebstahl, trailerschaeden, fahrerangriffe]  (per D-17)
- Body: Temperature-sensitive cargo vulnerabilities, diesel theft causing operational disruption of cooling unit, damage documentation for insurance (cooling chain break evidence), KONVOI protecting both cargo value and operational continuity.

**3. src/content/industry/de/intermodal.md** (per D-15, D-16)
- translationKey: intermodal
- canonicalKey: intermodal-de
- slug: intermodal
- title: Intermodaler Transport
- riskProfile: "Im intermodalen Transport wechseln Trailer zwischen Straße, Schiene und See — oft ohne konstante Fahrerkontrolle. Jeder Trägerwechsel ist ein potenzielles Sicherheitsrisiko und eine Haftungslücke."
- relatedUseCases: [transparenz-der-operationen, trailerschaeden, equipmentdiebstahl]  (per D-17)
- Body: Intermodal chain complexity (sea/rail/road), handover custody gaps, liability in multi-modal CMR chains, ghost shipments, KONVOI logging every coupling event regardless of transport mode.

**4. src/content/industry/de/sonstige.md** (per D-15, D-16)
- translationKey: other
- canonicalKey: other-de
- slug: sonstige
- title: Weitere Transporte
- riskProfile: "Sammelgut, Palettenware und allgemeine Stückgüter sind zwar pro Trailer weniger wertvoll als Hochpreisware — dafür aber häufiger Ziel von Gelegenheitsdiebstahl und Dieselklau auf ungesicherten Stellplätzen."
- relatedUseCases: [ladungsdiebstahl, dieseldiebstahl, standzeit-optimierung]  (per D-17)
- Body: General cargo vulnerability at rest areas, diesel theft as opportunistic crime, stationary time and depot visibility, KONVOI as cost-effective protection even for lower-value loads.

For each EN file, mirror DE structure with (per D-18, D-19):
- locale: en
- canonicalKey: {translationKey}-en
- slug: EN slug from routeMap.ts (high-value, cooling, intermodal, other)
- relatedUseCases: same DE slugs (the EN template in Plan 06 resolves these to EN hrefs via a
  hardcoded map — no change needed here)
- All copy in British English
- 200-300 word body (EN translation of DE)

EN titles:
- high-value.md: "High-Value Cargo"
- cooling.md: "Refrigerated Transport"
- intermodal.md: "Intermodal Transport"
- other.md: "General Cargo"

CRITICAL — generateId collision prevention: The industryCollection uses generateId =
`${locale}/${slug}`. The DE and EN intermodal entries will have IDs `de/intermodal` and
`en/intermodal` respectively. Ensure each file has its correct `locale` field set (de vs en)
so the generated IDs are distinct.
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && pnpm check 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ls src/content/industry/de/ returns 4 .md files
    - ls src/content/industry/en/ returns 4 .md files
    - grep -r "translationKey: high-value" src/content/industry/ returns exactly 2 matches (DE + EN)
    - grep -r "translationKey: intermodal" src/content/industry/ returns exactly 2 matches
    - grep -r "riskProfile" src/content/industry/de/ returns 4 matches (one per DE file)
    - grep -r "relatedUseCases" src/content/industry/de/ returns 4 matches
    - grep -r "slug: hochwertige-gueter" src/content/industry/de/ returns 1 match
    - grep -r "slug: high-value" src/content/industry/en/ returns 1 match
    - pnpm check exits 0 with no Zod schema errors on industry collection
    - pnpm run check:translations exits 0 (DE + EN parity for industry collection)
  </acceptance_criteria>
  <done>8 industry markdown files created with correct slugs, translationKeys, riskProfile, relatedUseCases cross-links (per D-15, D-16, D-17), and 200-300 word body copy. Industry collection passes Zod validation and translation parity check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author→collection | Markdown files authored by Claude, reviewed by user — no external input |
| collection→page | Zod schema validates all frontmatter at build time — invalid entries fail the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | relatedUseCases field values (DE slugs in both locales) | mitigate | Values are validated at build time by the dynamic page template's hardcoded map. Invalid DE slugs produce null (filtered, no crash). Verify with grep in acceptance criteria that slugs match routeMap.ts exactly. |
| T-04-07-02 | Information Disclosure | riskProfile field with financial figures | accept | All figures reference public industry data (TAPA 2024, general logistics statistics). No confidential data. |
| T-04-07-03 | Injection | Markdown body with HTML elements | accept | Astro's markdown renderer sanitizes HTML. Body is author-written content — no user input. |
| T-04-07-04 | Spoofing | generateId collision (DE/EN intermodal both use slug "intermodal") | mitigate | generateId = `${locale}/${slug}` produces de/intermodal and en/intermodal — distinct IDs. Acceptance criteria verify both locale values are set correctly. |
</threat_model>

<verification>
After task completes:
- `pnpm check` exits 0 (Zod validates all 8 industry collection entries)
- `pnpm run check:translations` exits 0 (DE+EN parity for industry)
- `ls src/content/industry/de/ | wc -l` returns 4
- `ls src/content/industry/en/ | wc -l` returns 4
- `grep -r "translationKey" src/content/industry/ | wc -l` returns 8 (each file has one)
</verification>

<success_criteria>
8 industry content collection files created with full copy (per D-15, D-16, D-17, D-18, D-19):
1. 4 DE industry entries — correct slugs, translationKeys, riskProfile, relatedUseCases cross-links
2. 4 EN industry entries — mirrored from DE, British English
All entries pass pnpm check Zod validation. Translation parity check green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-marketing-pages/04-07-SUMMARY.md`
</output>
