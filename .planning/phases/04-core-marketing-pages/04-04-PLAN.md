---
phase: 04-core-marketing-pages
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/islands/SensorDataViz.tsx
  - src/data/sensor-scenarios/cargo-theft.json
  - src/data/sensor-scenarios/diesel-theft.json
  - src/data/sensor-scenarios/equipment-theft.json
  - src/data/sensor-scenarios/operations-transparency.json
  - src/data/sensor-scenarios/trailer-damage.json
  - src/data/sensor-scenarios/driver-assaults.json
  - src/data/sensor-scenarios/stationary-time.json
autonomous: true
requirements:
  - UC-01
  - UC-02
  - UC-03
  - UC-04
  - UC-05

must_haves:
  truths:
    - "SensorDataViz.tsx is a Preact island that renders a uPlot time-series chart"
    - "Chart auto-plays on scroll-into-view using client:visible directive"
    - "Data traces draw left-to-right, then detection/classification/alarm phases highlight sequentially"
    - "All 7 scenario JSON fixture files exist in src/data/sensor-scenarios/"
    - "Each fixture has scenarioId, title, timeline array, and phases array"
  artifacts:
    - path: "src/components/islands/SensorDataViz.tsx"
      provides: "Preact island wrapping uPlot time-series chart"
      contains: "uPlot"
    - path: "src/data/sensor-scenarios/cargo-theft.json"
      provides: "Cargo theft sensor scenario fixture"
      contains: "cargo-theft"
    - path: "src/data/sensor-scenarios/diesel-theft.json"
      provides: "Diesel theft sensor scenario fixture"
    - path: "src/data/sensor-scenarios/equipment-theft.json"
      provides: "Equipment theft sensor scenario fixture"
    - path: "src/data/sensor-scenarios/operations-transparency.json"
      provides: "Operations transparency scenario fixture"
    - path: "src/data/sensor-scenarios/trailer-damage.json"
      provides: "Trailer damage scenario fixture"
    - path: "src/data/sensor-scenarios/driver-assaults.json"
      provides: "Driver assaults scenario fixture"
    - path: "src/data/sensor-scenarios/stationary-time.json"
      provides: "Stationary time optimisation scenario fixture"
  key_links:
    - from: "src/components/islands/SensorDataViz.tsx"
      to: "src/data/sensor-scenarios/{scenarioId}.json"
      via: "fetch('/data/sensor-scenarios/{scenarioId}.json') in useEffect"
      pattern: "fetch.*sensor-scenarios"
    - from: "src/pages/anwendungen/[slug].astro"
      to: "src/components/islands/SensorDataViz.tsx"
      via: "<SensorDataViz scenarioId={entry.data.canonicalKey} client:visible />"
      pattern: "SensorDataViz"
---

<objective>
Build the SensorDataViz Preact island (wrapping uPlot) and create all 7 sensor scenario JSON fixture files. This island is the primary interactive element on every use-case page — it visualises how sensor data builds toward a threat classification.

Purpose: The timeline chart tells the prevention story visually. It is the single new component that must be built from scratch in Phase 4 (all other pages reuse existing widgets).

Output: src/components/islands/SensorDataViz.tsx with animated uPlot chart; 7 JSON fixtures in src/data/sensor-scenarios/; the component is usable as <SensorDataViz scenarioId="cargo-theft" client:visible />.
</objective>

<execution_context>
@/c/Users/Rami/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/Rami/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-core-marketing-pages/04-CONTEXT.md
@.planning/phases/04-core-marketing-pages/04-RESEARCH.md
@src/data/brand/voice.md
@astro.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SensorDataViz Preact island with uPlot (D-04, D-05, UC-03)</name>
  <files>src/components/islands/SensorDataViz.tsx</files>
  <read_first>
    - package.json (verify @astrojs/preact and uplot are present after Plan 01)
    - astro.config.ts (verify preact() integration is registered after Plan 01)
    - src/components/islands/ (check directory exists; create if absent)
  </read_first>
  <action>
Create the directory `src/components/islands/` if it does not exist, then write `src/components/islands/SensorDataViz.tsx`.

**Fixture JSON schema** (used by the chart — canonical format for all 7 files):
```typescript
interface SensorFixture {
  scenarioId: string;                  // canonical slug matching routeMap.ts
  title: string;                       // English title for aria/accessibility
  description: string;                 // Brief scenario description
  timeline: Array<{
    t: number;                         // Unix timestamp in seconds (not ms)
    motion: number;                    // 0–100 motion intensity
    shock: number;                     // 0–100 shock intensity
    gps: number;                       // 0–1 GPS anomaly flag (as number for uPlot)
  }>;
  phases: Array<{
    name: string;                      // 'Detection' | 'Classification' | 'Measures'
    start: number;                     // Unix timestamp in seconds
    end: number;                       // Unix timestamp in seconds
    color: string;                     // hex colour for phase highlight band
  }>;
}
```

**Component implementation** — create `src/components/islands/SensorDataViz.tsx`:

```tsx
// src/components/islands/SensorDataViz.tsx
// Preact island — hydrated client:visible (scroll-into-view).
// Renders a uPlot time-series chart showing motion/shock/GPS sensor traces
// for a given scenario, then animates detection/classification/alarm phases.
// Per D-04: time-series line/area chart. Per D-05: animated playback.

import { useEffect, useRef, useState } from 'preact/hooks';
import uPlot from 'uplot';
import 'uplot/dist/uPlot.min.css';

interface TimelinePoint {
  t: number;
  motion: number;
  shock: number;
  gps: number;
}

interface Phase {
  name: string;
  start: number;
  end: number;
  color: string;
}

interface SensorFixture {
  scenarioId: string;
  title: string;
  description: string;
  timeline: Array<TimelinePoint>;
  phases: Array<Phase>;
}

interface Props {
  scenarioId: string;
  locale?: string;
}

// Phase label translations
const phaseLabels: Record<string, Record<string, string>> = {
  Detection:      { de: 'Erkennung',      en: 'Detection' },
  Classification: { de: 'Klassifizierung', en: 'Classification' },
  Measures:       { de: 'Maßnahmen',      en: 'Measures' },
};

export default function SensorDataViz({ scenarioId, locale = 'de' }: Props) {
  const containerRef = useRef<HTMLDivElement>(null);
  const chartRef = useRef<uPlot | null>(null);
  const [fixture, setFixture] = useState<SensorFixture | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [activePhase, setActivePhase] = useState<number>(-1);

  // Fetch scenario fixture on mount
  useEffect(() => {
    const url = `/data/sensor-scenarios/${scenarioId}.json`;
    fetch(url)
      .then((r) => {
        if (!r.ok) throw new Error(`Failed to load scenario: ${r.status}`);
        return r.json() as Promise<SensorFixture>;
      })
      .then(setFixture)
      .catch((e: Error) => setError(e.message));
  }, [scenarioId]);

  // Build uPlot chart when fixture is loaded and container is ready
  useEffect(() => {
    if (!fixture || !containerRef.current) return;

    // Clean up previous chart instance
    if (chartRef.current) {
      chartRef.current.destroy();
      chartRef.current = null;
    }

    const ts = fixture.timeline.map((p) => p.t);
    const motion = fixture.timeline.map((p) => p.motion);
    const shock = fixture.timeline.map((p) => p.shock);
    const gps = fixture.timeline.map((p) => p.gps * 100); // scale 0-1 flag to 0-100

    const data: uPlot.AlignedData = [ts, motion, shock, gps];

    const opts: uPlot.Options = {
      title: fixture.title,
      width: containerRef.current.offsetWidth || 800,
      height: 320,
      cursor: { show: true },
      legend: { show: true },
      scales: {
        x: { time: true },
        y: { range: [0, 110] },
      },
      axes: [
        { label: '' },
        { label: 'Intensität (0–100)', labelSize: 14 },
      ],
      series: [
        {},
        {
          label: locale === 'de' ? 'Bewegung' : 'Motion',
          stroke: '#3b82f6',
          fill: 'rgba(59,130,246,0.12)',
          width: 2,
        },
        {
          label: locale === 'de' ? 'Schock' : 'Shock',
          stroke: '#f59e0b',
          fill: 'rgba(245,158,11,0.12)',
          width: 2,
        },
        {
          label: 'GPS',
          stroke: '#10b981',
          fill: 'rgba(16,185,129,0.10)',
          width: 2,
          dash: [4, 4],
        },
      ],
      hooks: {
        drawAxes: [
          (u) => {
            // Draw phase highlight bands behind the data
            const ctx = u.ctx;
            fixture.phases.forEach((phase) => {
              const x0 = Math.round(u.valToPos(phase.start, 'x', true));
              const x1 = Math.round(u.valToPos(phase.end, 'x', true));
              const top = u.bbox.top;
              const height = u.bbox.height;
              ctx.save();
              ctx.fillStyle = phase.color + '33'; // 20% opacity
              ctx.fillRect(x0, top, x1 - x0, height);
              // Phase label
              ctx.fillStyle = phase.color;
              ctx.font = '11px sans-serif';
              ctx.textAlign = 'center';
              const label = phaseLabels[phase.name]?.[locale] ?? phase.name;
              ctx.fillText(label, (x0 + x1) / 2, top + 14);
              ctx.restore();
            });
          },
        ],
      },
    };

    chartRef.current = new uPlot(opts, data, containerRef.current);

    // Animate phase highlights sequentially after 600ms delay
    fixture.phases.forEach((_, idx) => {
      setTimeout(() => setActivePhase(idx), 600 + idx * 900);
    });

    return () => {
      chartRef.current?.destroy();
      chartRef.current = null;
    };
  }, [fixture, locale]);

  // Resize handler
  useEffect(() => {
    const handleResize = () => {
      if (chartRef.current && containerRef.current) {
        chartRef.current.setSize({
          width: containerRef.current.offsetWidth,
          height: 320,
        });
      }
    };
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  if (error) {
    return (
      <div class="rounded-lg border border-red-200 bg-red-50 p-6 text-center text-sm text-red-700 dark:border-red-800 dark:bg-red-950 dark:text-red-300">
        {locale === 'de'
          ? 'Sensor-Daten konnten nicht geladen werden.'
          : 'Sensor data could not be loaded.'}
      </div>
    );
  }

  if (!fixture) {
    return (
      <div class="flex h-[320px] items-center justify-center rounded-lg border border-slate-200 bg-slate-50 dark:border-slate-700 dark:bg-slate-900">
        <span class="text-sm text-slate-500 dark:text-slate-400">
          {locale === 'de' ? 'Laden...' : 'Loading...'}
        </span>
      </div>
    );
  }

  return (
    <div class="sensor-data-viz w-full overflow-x-auto rounded-lg border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900">
      <div class="mb-3">
        <p class="text-sm text-slate-500 dark:text-slate-400">{fixture.description}</p>
      </div>
      <div ref={containerRef} class="w-full" aria-label={fixture.title} />
      {/* Phase indicator dots below chart */}
      <div class="mt-3 flex gap-4 justify-center text-xs text-slate-500">
        {fixture.phases.map((phase, idx) => (
          <span
            key={phase.name}
            class={`flex items-center gap-1 transition-opacity duration-500 ${idx <= activePhase ? 'opacity-100' : 'opacity-30'}`}
          >
            <span
              class="inline-block h-2 w-2 rounded-full"
              style={{ backgroundColor: phase.color }}
            />
            {phaseLabels[phase.name]?.[locale] ?? phase.name}
          </span>
        ))}
      </div>
    </div>
  );
}
```

**Note on CSS import:** `import 'uplot/dist/uPlot.min.css'` — this CSS is required for uPlot to render correctly. Astro + Vite handles CSS imports in `.tsx` files natively. If the build fails with a CSS import error, replace with an inline `<style>` tag containing the minimal uPlot reset (just the `.u-wrap`, `.u-over`, `.u-under` positional rules).

**Pitfall avoidance (from RESEARCH.md Pitfall 1):** The `useEffect` + `useState` pattern ensures the chart only initialises after mount and data fetch, never before. The `client:visible` directive in the page template ensures the island hydrates only when scrolled into view.
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/islands/SensorDataViz.tsx` — file exists
    - `grep "uPlot" src/components/islands/SensorDataViz.tsx` returns a match
    - `grep "uplot/dist/uPlot.min.css" src/components/islands/SensorDataViz.tsx` returns a match (CSS import present)
    - `grep "client:visible" src/components/islands/SensorDataViz.tsx` returns 0 (the directive is used by the page, not the component itself — component file should NOT contain client:visible)
    - `grep "useEffect" src/components/islands/SensorDataViz.tsx` returns at least 2 matches (fetch + chart init)
    - `grep "fetch.*sensor-scenarios" src/components/islands/SensorDataViz.tsx` returns a match
    - `grep "phases" src/components/islands/SensorDataViz.tsx` returns a match (phase bands rendered)
    - pnpm build exits 0 (catches CSS bundling issues with uPlot CSS import)
  </acceptance_criteria>
  <done>SensorDataViz.tsx exists as Preact island with uPlot chart, fetch-based fixture loading, and phase animation; pnpm build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create 7 sensor scenario fixture JSON files (D-06, UC-04)</name>
  <files>
    src/data/sensor-scenarios/cargo-theft.json,
    src/data/sensor-scenarios/diesel-theft.json,
    src/data/sensor-scenarios/equipment-theft.json,
    src/data/sensor-scenarios/operations-transparency.json,
    src/data/sensor-scenarios/trailer-damage.json,
    src/data/sensor-scenarios/driver-assaults.json,
    src/data/sensor-scenarios/stationary-time.json
  </files>
  <read_first>
    - .planning/current-site-overview.md (§4 Use cases — cost anchors and scenario descriptions for each use case)
    - .planning/phases/04-core-marketing-pages/04-RESEARCH.md (Example 2: Sensor Scenario Fixture for schema reference)
    - src/components/islands/SensorDataViz.tsx (just created — verify the exact schema fields expected: t, motion, shock, gps)
  </read_first>
  <action>
Create `src/data/sensor-scenarios/` directory if it does not exist, then write all 7 fixture files.

Each fixture uses Unix timestamps starting at a base value. Use base `t = 1713870000` (2024-04-23 10:00:00 UTC) and add 60-second intervals. Timeline values are on 0–100 scale. Phases use the detection→classification→measures colour scheme:
- Detection: `"#f59e0b"` (amber)
- Classification: `"#3b82f6"` (blue)
- Measures: `"#10b981"` (green)

**1. `src/data/sensor-scenarios/cargo-theft.json`**:
```json
{
  "scenarioId": "cargo-theft",
  "title": "Cargo Theft Prevention Scenario",
  "description": "Sensor trace of a prevented cargo theft — trailer parked at a motorway rest area",
  "timeline": [
    { "t": 1713870000, "motion": 4, "shock": 2, "gps": 0 },
    { "t": 1713870060, "motion": 5, "shock": 3, "gps": 0 },
    { "t": 1713870120, "motion": 6, "shock": 2, "gps": 0 },
    { "t": 1713870180, "motion": 8, "shock": 4, "gps": 0 },
    { "t": 1713870240, "motion": 22, "shock": 12, "gps": 0 },
    { "t": 1713870300, "motion": 48, "shock": 28, "gps": 0 },
    { "t": 1713870360, "motion": 71, "shock": 52, "gps": 0 },
    { "t": 1713870420, "motion": 88, "shock": 74, "gps": 1 },
    { "t": 1713870480, "motion": 10, "shock": 5, "gps": 0 },
    { "t": 1713870540, "motion": 3, "shock": 1, "gps": 0 }
  ],
  "phases": [
    { "name": "Detection",      "start": 1713870240, "end": 1713870360, "color": "#f59e0b" },
    { "name": "Classification", "start": 1713870360, "end": 1713870420, "color": "#3b82f6" },
    { "name": "Measures",       "start": 1713870420, "end": 1713870480, "color": "#10b981" }
  ]
}
```

**2. `src/data/sensor-scenarios/diesel-theft.json`**:
```json
{
  "scenarioId": "diesel-theft",
  "title": "Diesel Theft Prevention Scenario",
  "description": "Sensor trace showing tank tampering — shock pattern consistent with fuel siphoning",
  "timeline": [
    { "t": 1713870000, "motion": 3, "shock": 1, "gps": 0 },
    { "t": 1713870060, "motion": 4, "shock": 2, "gps": 0 },
    { "t": 1713870120, "motion": 5, "shock": 3, "gps": 0 },
    { "t": 1713870180, "motion": 6, "shock": 14, "gps": 0 },
    { "t": 1713870240, "motion": 8, "shock": 38, "gps": 0 },
    { "t": 1713870300, "motion": 10, "shock": 62, "gps": 0 },
    { "t": 1713870360, "motion": 12, "shock": 79, "gps": 0 },
    { "t": 1713870420, "motion": 9, "shock": 81, "gps": 1 },
    { "t": 1713870480, "motion": 5, "shock": 20, "gps": 0 },
    { "t": 1713870540, "motion": 3, "shock": 2, "gps": 0 }
  ],
  "phases": [
    { "name": "Detection",      "start": 1713870180, "end": 1713870300, "color": "#f59e0b" },
    { "name": "Classification", "start": 1713870300, "end": 1713870420, "color": "#3b82f6" },
    { "name": "Measures",       "start": 1713870420, "end": 1713870480, "color": "#10b981" }
  ]
}
```

**3. `src/data/sensor-scenarios/equipment-theft.json`**:
```json
{
  "scenarioId": "equipment-theft",
  "title": "Equipment Theft Prevention Scenario",
  "description": "Sensor trace showing tyre and trailer equipment removal attempt",
  "timeline": [
    { "t": 1713870000, "motion": 2, "shock": 1, "gps": 0 },
    { "t": 1713870060, "motion": 3, "shock": 2, "gps": 0 },
    { "t": 1713870120, "motion": 5, "shock": 18, "gps": 0 },
    { "t": 1713870180, "motion": 9, "shock": 44, "gps": 0 },
    { "t": 1713870240, "motion": 18, "shock": 66, "gps": 0 },
    { "t": 1713870300, "motion": 25, "shock": 82, "gps": 0 },
    { "t": 1713870360, "motion": 30, "shock": 88, "gps": 1 },
    { "t": 1713870420, "motion": 6, "shock": 10, "gps": 0 },
    { "t": 1713870480, "motion": 2, "shock": 2, "gps": 0 }
  ],
  "phases": [
    { "name": "Detection",      "start": 1713870120, "end": 1713870240, "color": "#f59e0b" },
    { "name": "Classification", "start": 1713870240, "end": 1713870360, "color": "#3b82f6" },
    { "name": "Measures",       "start": 1713870360, "end": 1713870420, "color": "#10b981" }
  ]
}
```

**4. `src/data/sensor-scenarios/operations-transparency.json`**:
```json
{
  "scenarioId": "operations-transparency",
  "title": "Operations Transparency Scenario",
  "description": "Sensor trace documenting an unauthorised coupling process by a third party",
  "timeline": [
    { "t": 1713870000, "motion": 2, "shock": 1, "gps": 0 },
    { "t": 1713870060, "motion": 3, "shock": 2, "gps": 0 },
    { "t": 1713870120, "motion": 28, "shock": 8, "gps": 0 },
    { "t": 1713870180, "motion": 55, "shock": 14, "gps": 1 },
    { "t": 1713870240, "motion": 72, "shock": 18, "gps": 1 },
    { "t": 1713870300, "motion": 68, "shock": 15, "gps": 1 },
    { "t": 1713870360, "motion": 44, "shock": 10, "gps": 1 },
    { "t": 1713870420, "motion": 12, "shock": 4, "gps": 0 },
    { "t": 1713870480, "motion": 4, "shock": 2, "gps": 0 }
  ],
  "phases": [
    { "name": "Detection",      "start": 1713870120, "end": 1713870180, "color": "#f59e0b" },
    { "name": "Classification", "start": 1713870180, "end": 1713870300, "color": "#3b82f6" },
    { "name": "Measures",       "start": 1713870300, "end": 1713870420, "color": "#10b981" }
  ]
}
```

**5. `src/data/sensor-scenarios/trailer-damage.json`**:
```json
{
  "scenarioId": "trailer-damage",
  "title": "Trailer Damage Detection Scenario",
  "description": "Sensor trace capturing a collision event and impact reconstruction for insurance",
  "timeline": [
    { "t": 1713870000, "motion": 15, "shock": 5, "gps": 0 },
    { "t": 1713870060, "motion": 18, "shock": 6, "gps": 0 },
    { "t": 1713870120, "motion": 20, "shock": 7, "gps": 0 },
    { "t": 1713870180, "motion": 22, "shock": 92, "gps": 0 },
    { "t": 1713870240, "motion": 25, "shock": 78, "gps": 0 },
    { "t": 1713870300, "motion": 19, "shock": 42, "gps": 0 },
    { "t": 1713870360, "motion": 16, "shock": 18, "gps": 0 },
    { "t": 1713870420, "motion": 14, "shock": 8, "gps": 0 },
    { "t": 1713870480, "motion": 12, "shock": 5, "gps": 0 }
  ],
  "phases": [
    { "name": "Detection",      "start": 1713870180, "end": 1713870240, "color": "#f59e0b" },
    { "name": "Classification", "start": 1713870240, "end": 1713870300, "color": "#3b82f6" },
    { "name": "Measures",       "start": 1713870300, "end": 1713870360, "color": "#10b981" }
  ]
}
```

**6. `src/data/sensor-scenarios/driver-assaults.json`**:
```json
{
  "scenarioId": "driver-assaults",
  "title": "Driver Assault Prevention Scenario",
  "description": "Sensor trace showing approach and deterrence of a violent threat to the driver",
  "timeline": [
    { "t": 1713870000, "motion": 3, "shock": 1, "gps": 0 },
    { "t": 1713870060, "motion": 4, "shock": 2, "gps": 0 },
    { "t": 1713870120, "motion": 6, "shock": 4, "gps": 0 },
    { "t": 1713870180, "motion": 35, "shock": 22, "gps": 0 },
    { "t": 1713870240, "motion": 68, "shock": 55, "gps": 0 },
    { "t": 1713870300, "motion": 82, "shock": 71, "gps": 1 },
    { "t": 1713870360, "motion": 90, "shock": 78, "gps": 1 },
    { "t": 1713870420, "motion": 15, "shock": 8, "gps": 0 },
    { "t": 1713870480, "motion": 4, "shock": 2, "gps": 0 }
  ],
  "phases": [
    { "name": "Detection",      "start": 1713870180, "end": 1713870300, "color": "#f59e0b" },
    { "name": "Classification", "start": 1713870300, "end": 1713870360, "color": "#3b82f6" },
    { "name": "Measures",       "start": 1713870360, "end": 1713870420, "color": "#10b981" }
  ]
}
```

**7. `src/data/sensor-scenarios/stationary-time.json`**:
```json
{
  "scenarioId": "stationary-time",
  "title": "Stationary Time Optimisation Scenario",
  "description": "Sensor trace logging extended stationary period with documented stop events for fleet transparency",
  "timeline": [
    { "t": 1713870000, "motion": 2, "shock": 1, "gps": 0 },
    { "t": 1713870060, "motion": 2, "shock": 1, "gps": 0 },
    { "t": 1713870120, "motion": 3, "shock": 1, "gps": 0 },
    { "t": 1713870180, "motion": 2, "shock": 1, "gps": 0 },
    { "t": 1713870240, "motion": 2, "shock": 2, "gps": 0 },
    { "t": 1713870300, "motion": 3, "shock": 1, "gps": 0 },
    { "t": 1713870360, "motion": 18, "shock": 6, "gps": 1 },
    { "t": 1713870420, "motion": 42, "shock": 12, "gps": 1 },
    { "t": 1713870480, "motion": 15, "shock": 4, "gps": 0 }
  ],
  "phases": [
    { "name": "Detection",      "start": 1713870000, "end": 1713870300, "color": "#f59e0b" },
    { "name": "Classification", "start": 1713870300, "end": 1713870360, "color": "#3b82f6" },
    { "name": "Measures",       "start": 1713870360, "end": 1713870480, "color": "#10b981" }
  ]
}
```

**Important:** The fixture files live in `src/data/sensor-scenarios/` at build time but the Preact island fetches them at runtime via `/data/sensor-scenarios/{id}.json`. This requires the JSON files to also be copied to `public/data/sensor-scenarios/` so they are served as static assets. Create `public/data/sensor-scenarios/` directory and copy all 7 JSON files there as well.

Alternatively — and more maintainably — use a build script or just place the files only in `public/data/sensor-scenarios/`. The `src/data/sensor-scenarios/` path is used conceptually (the RESEARCH.md reference), but since they are not imported as Astro content collections, the runtime fetch requires them to be in `public/`. Place the canonical copies in **`public/data/sensor-scenarios/`** and skip the `src/data/sensor-scenarios/` directory (the RESEARCH.md reference was aspirational — the fetch URL `/data/sensor-scenarios/` resolves to `public/data/sensor-scenarios/`).

Create all 7 files at `public/data/sensor-scenarios/{scenarioId}.json`.
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && ls public/data/sensor-scenarios/ && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/data/sensor-scenarios/ | wc -l` returns 7
    - `test -f public/data/sensor-scenarios/cargo-theft.json` — exists
    - `test -f public/data/sensor-scenarios/diesel-theft.json` — exists
    - `test -f public/data/sensor-scenarios/equipment-theft.json` — exists
    - `test -f public/data/sensor-scenarios/operations-transparency.json` — exists
    - `test -f public/data/sensor-scenarios/trailer-damage.json` — exists
    - `test -f public/data/sensor-scenarios/driver-assaults.json` — exists
    - `test -f public/data/sensor-scenarios/stationary-time.json` — exists
    - `grep "scenarioId" public/data/sensor-scenarios/cargo-theft.json` returns a match
    - `grep "phases" public/data/sensor-scenarios/cargo-theft.json` returns a match
    - `pnpm build` exits 0
    - After build: `test -f dist/data/sensor-scenarios/cargo-theft.json` — copied to dist/
  </acceptance_criteria>
  <done>All 7 scenario fixtures exist in public/data/sensor-scenarios/; pnpm build exits 0; fixtures copied to dist/data/sensor-scenarios/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → public/data/sensor-scenarios/*.json | Preact island fetches JSON from same-origin static path |
| uPlot canvas rendering | Client-side chart library renders data to canvas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | fetch() call in SensorDataViz | accept | Fetch targets same-origin static JSON (no cross-origin); JSON is committed to the repo and cannot be tampered with at runtime by users; no user input flows into the fetch URL (scenarioId is a prop passed from the Astro page template at build time) |
| T-04-04-02 | Tampering | uPlot canvas XSS | accept | uPlot renders to a Canvas element; Canvas API cannot inject DOM nodes or execute scripts; no set:html on chart output |
| T-04-04-03 | Disclosure | Sensor fixture data | accept | Fixture data is synthetic/illustrative scenario data, not real customer sensor readings; no PII, no real event data |
| T-04-04-04 | Denial of Service | Missing fixture file (fetch 404) | mitigate | SensorDataViz renders an error state on fetch failure (not a crash); every use-case page is responsible for passing a valid scenarioId matching a file in public/data/sensor-scenarios/ — validated by cross-link script in Plan 07 |
</threat_model>

<verification>
After both tasks:
1. `test -f src/components/islands/SensorDataViz.tsx` passes
2. `ls public/data/sensor-scenarios/ | wc -l` returns 7
3. `pnpm build` exits 0
4. `test -f dist/data/sensor-scenarios/cargo-theft.json` passes (static file copied to dist)
5. `grep "uPlot" src/components/islands/SensorDataViz.tsx` returns a match
6. `grep "client:visible" src/components/islands/SensorDataViz.tsx` returns 0 lines (directive belongs in the page template)
</verification>

<success_criteria>
- SensorDataViz.tsx is a complete Preact island: fetches fixture JSON, renders uPlot time-series chart, animates phase bands, handles loading/error states
- All 7 scenario fixtures exist at public/data/sensor-scenarios/ and are copied to dist/ by pnpm build
- pnpm build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-marketing-pages/04-04-SUMMARY.md` following the summary template.
</output>
