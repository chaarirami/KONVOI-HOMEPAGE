---
phase: 04-core-marketing-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/pages/produkt.astro
  - src/pages/en/product.astro
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05

must_haves:
  truths:
    - "DE product page exists at /produkt and EN product page at /en/product"
    - "Page covers hardware spec, sensor-positions diagram placeholder, Detection/Classification/Measures steps, Camera Module + Logbook add-ons, 120-minute install promise"
    - "Both pages build to dist/"
  artifacts:
    - path: "src/pages/produkt.astro"
      provides: "DE product page with 5 sections (PROD-01 through PROD-05)"
      contains: "120 Minuten"
    - path: "src/pages/en/product.astro"
      provides: "EN product page with 5 sections"
      contains: "120 minutes"
    - path: "public/images/sensor-positions-placeholder.svg"
      provides: "Placeholder for trailer sensor positions diagram (PROD-02)"
  key_links:
    - from: "src/pages/produkt.astro"
      to: "src/i18n/translations.ts"
      via: "t() helper for all section headings"
      pattern: "t\\('product\\."
    - from: "src/pages/produkt.astro"
      to: "src/data/brand/canonical.yaml"
      via: "install promise and tier names sourced from canonical.yaml"
      pattern: "canonical.yaml"
---

<objective>
Build the DE product page (src/pages/produkt.astro) and EN product page (src/pages/en/product.astro) covering all five PROD requirements: hardware spec, sensor-positions diagram, Detection→Classification→Measures steps, add-on modules, and the 120-minute install promise.

Purpose: The product page is the primary destination for visitors who click "Produkt" in the nav. It must explain the full system clearly and funnel to a consult request.

Output: Two product page files with real Konvoi copy; placeholder diagram SVG; both build to dist/produkt/index.html and dist/en/product/index.html.
</objective>

<execution_context>
@/c/Users/Rami/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/Rami/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-core-marketing-pages/04-CONTEXT.md
@.planning/current-site-overview.md
@src/data/brand/voice.md
@src/data/brand/canonical.yaml
@src/i18n/translations.ts
@src/components/widgets/Hero.astro
@src/components/widgets/Content.astro
@src/components/widgets/Steps.astro
@src/components/widgets/Features.astro
@src/components/widgets/CallToAction.astro
@src/types.d.ts
</context>

<interfaces>
<!-- Key widget props for executor reference -->

Steps.astro Props:
```typescript
export interface Steps {
  id?: string;
  isDark?: boolean;
  tagline?: string;
  title?: string;
  subtitle?: string;
  items: Array<{
    title: string;
    description?: string;
    icon?: string;
  }>;
  image?: string | { src: string; alt: string };
  callToAction?: CallToAction;
  isReversed?: boolean;
}
```

Content.astro Props:
```typescript
export interface Content {
  id?: string;
  isDark?: boolean;
  tagline?: string;
  title?: string;
  subtitle?: string;
  content?: string;
  items?: Array<{
    title?: string;
    description?: string;
    icon?: string;
  }>;
  image?: string | { src: string; alt: string };
  isReversed?: boolean;
  isAfterContent?: boolean;
  callToAction?: CallToAction;
}
```

Features2.astro Props (2-column layout — good for add-ons):
```typescript
export interface Features2 {
  id?: string;
  isDark?: boolean;
  tagline?: string;
  title?: string;
  subtitle?: string;
  items?: Array<{
    title: string;
    description?: string;
    icon?: string;
  }>;
  columns?: number;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build DE product page src/pages/produkt.astro (PROD-01 to PROD-05)</name>
  <files>src/pages/produkt.astro, public/images/sensor-positions-placeholder.svg</files>
  <read_first>
    - src/data/brand/canonical.yaml (install.max_minutes = 120, pricing.tiers for add-on names)
    - src/data/brand/voice.md (approved verbs, formal Sie, no exclamation points)
    - .planning/current-site-overview.md (§2 Product, §7 /the-solution/ and /the-solution/hardware/ for spec copy)
    - src/i18n/translations.ts (product.* keys added in Plan 01)
    - src/components/widgets/Steps.astro (confirm items prop structure)
    - src/layouts/PageLayout.astro (verify import path)
  </read_first>
  <action>
**Step 1 — Create the sensor-positions placeholder SVG** at `public/images/sensor-positions-placeholder.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 400" style="font-family:sans-serif;background:#f0f4f8">
  <rect width="800" height="400" fill="#f0f4f8" rx="8"/>
  <!-- Simplified trailer outline -->
  <rect x="80" y="120" width="600" height="180" fill="none" stroke="#334155" stroke-width="3" rx="4"/>
  <rect x="60" y="150" width="30" height="120" fill="none" stroke="#334155" stroke-width="2" rx="2"/>
  <!-- Sensor position markers -->
  <circle cx="200" cy="130" r="12" fill="#3b82f6" opacity="0.8"/>
  <text x="200" y="110" text-anchor="middle" font-size="11" fill="#1e40af">Vorne links</text>
  <circle cx="520" cy="130" r="12" fill="#3b82f6" opacity="0.8"/>
  <text x="520" y="110" text-anchor="middle" font-size="11" fill="#1e40af">Vorne rechts</text>
  <circle cx="340" cy="295" r="12" fill="#10b981" opacity="0.8"/>
  <text x="340" y="325" text-anchor="middle" font-size="11" fill="#065f46">GPS Haupt</text>
  <circle cx="130" cy="210" r="10" fill="#f59e0b" opacity="0.8"/>
  <text x="130" y="245" text-anchor="middle" font-size="10" fill="#92400e">Schock</text>
  <circle cx="620" cy="210" r="10" fill="#f59e0b" opacity="0.8"/>
  <text x="620" y="245" text-anchor="middle" font-size="10" fill="#92400e">Schock</text>
  <!-- Legend -->
  <circle cx="90" cy="370" r="7" fill="#3b82f6"/>
  <text x="104" y="374" font-size="11" fill="#334155">Bewegungssensor</text>
  <circle cx="220" cy="370" r="7" fill="#10b981"/>
  <text x="234" y="374" font-size="11" fill="#334155">GPS-Modul</text>
  <circle cx="320" cy="370" r="7" fill="#f59e0b"/>
  <text x="334" y="374" font-size="11" fill="#334155">Schocksensor</text>
  <text x="400" y="60" text-anchor="middle" font-size="14" fill="#64748b">Sensorpositionen am Trailer — Platzhalter (wird vor Launch ersetzt)</text>
</svg>
```

**Step 2 — Create `src/pages/produkt.astro`**:

```astro
---
import PageLayout from '~/layouts/PageLayout.astro';
import Hero from '~/components/widgets/Hero.astro';
import Content from '~/components/widgets/Content.astro';
import Steps from '~/components/widgets/Steps.astro';
import Features2 from '~/components/widgets/Features2.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';
import { t } from '~/i18n/translations';
import brand from '~/data/brand/canonical.yaml';

const locale = 'de';

const metadata = {
  title: 'KONVOI Produkt — Sensor-basiertes Sicherheitssystem für Trailer',
  description:
    'Das KONVOI System: GPS, Schock- und Bewegungssensoren, KI-Klassifizierung und sofortige Abschreckungskette. Installation in unter 120 Minuten.',
};
---

<PageLayout metadata={metadata}>

  {/* Hero — PROD-01 */}
  <Hero
    tagline={t('product.hero_tagline', locale)}
    title={t('product.hero_title', locale)}
    subtitle={t('product.hero_subtitle', locale)}
    videoPoster="/images/hero-poster.jpg"
    actions={[
      {
        variant: 'primary',
        text: t('cta.book_consult', locale),
        href: '#consult',
      },
    ]}
  />

  {/* Hardware spec + sensor positions diagram — PROD-01, PROD-02 */}
  <Content
    id="hardware"
    tagline={t('product.hardware_tagline', locale)}
    title={t('product.hardware_title', locale)}
    subtitle={t('product.hardware_subtitle', locale)}
    items={[
      {
        title: 'GPS + LTE Konnektivität',
        description: 'Echtzeit-Ortung und Datenübertragung auch in Gebieten mit schwachem Signal.',
        icon: 'tabler:map-pin',
      },
      {
        title: 'Schock- und Bewegungssensoren',
        description: 'Erkennen verdächtige Muster — von abnormalen Türbewegungen bis zu unerwarteten Erschütterungen.',
        icon: 'tabler:wave-sine',
      },
      {
        title: '7-Tage Akku',
        description: 'Selbstversorgend. Kein Anschluss an die Fahrzeugelektrik nötig. Funktioniert auch wenn der Trailer abgekoppelt ist.',
        icon: 'tabler:battery',
      },
      {
        title: 'Verdecktes Gehäuse',
        description: 'Kompakt und unauffällig montiert. Kein sichtbarer Hinweis auf das Sicherheitssystem für potenzielle Diebe.',
        icon: 'tabler:eye-off',
      },
    ]}
    image={{
      src: '/images/sensor-positions-placeholder.svg',
      alt: 'Sensorpositionen am Trailer — KONVOI Systemübersicht',
    }}
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Content>

  {/* Detection → Classification → Measures — PROD-03 */}
  <Steps
    id="so-funktioniert-es"
    tagline={t('product.steps_tagline', locale)}
    title={t('product.steps_title', locale)}
    items={[
      {
        title: 'Erkennung',
        description:
          'Schock-, Bewegungs- und GPS-Sensoren erfassen kontinuierlich Daten. Anomalien werden sofort registriert — z. B. abnormale Türbewegungen um 2 Uhr nachts auf einem Rastplatz.',
        icon: 'tabler:radar',
      },
      {
        title: 'Klassifizierung',
        description:
          'Die KONVOI KI analysiert die Sensormuster und klassifiziert das Ereignis als Bedrohung oder Fehlalarm. Kein menschliches Eingreifen nötig — die Entscheidung dauert Sekunden.',
        icon: 'tabler:brain',
      },
      {
        title: 'Maßnahmen',
        description:
          'Bei bestätigter Bedrohung löst KONVOI sofort die Abschreckungskette aus: Sirene, Benachrichtigung des Fahrers, Alarmierung der Einsatzkräfte. Der Diebstahl wird verhindert, nicht nur dokumentiert.',
        icon: 'tabler:shield-check',
      },
    ]}
  />

  {/* Add-on modules: Camera Module + Logbook — PROD-04 */}
  <Features2
    id="erweiterungen"
    tagline={t('product.addons_tagline', locale)}
    title={t('product.addons_title', locale)}
    subtitle="Ergänzen Sie das KONVOI Standard-System nach Bedarf."
    columns={2}
    items={[
      {
        title: brand.pricing.tiers[1].de_name,
        description:
          `${brand.pricing.tiers[1].de_description} — Bei jedem klassifizierten Ereignis wird automatisch ein Video-Clip in der Cloud gespeichert. Lückenlose Beweissicherung für Versicherung und Ermittlungsbehörden.`,
        icon: 'tabler:video',
      },
      {
        title: brand.pricing.tiers[2].de_name,
        description:
          `${brand.pricing.tiers[2].de_description} — Ideal für Compliance-Anforderungen und die Kommunikation mit Auftraggebern. Jede Fahrt transparent dokumentiert.`,
        icon: 'tabler:file-description',
      },
    ]}
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Features2>

  {/* Installation + 120-minute promise + video slot — PROD-05 */}
  <Content
    id="installation"
    tagline={t('product.install_tagline', locale)}
    title={t('product.install_title', locale)}
    isReversed
    items={[
      {
        title: 'Eigenes Installationsteam',
        description: 'Kein Fremddienstleister. Unser Team kennt das System in- und auswendig und garantiert eine saubere, verdeckte Montage.',
        icon: 'tabler:tool',
      },
      {
        title: brand.install.de_promise,
        description: 'Kein Trailer muss den Betrieb länger als nötig unterbrechen. Keine Terminabhängigkeit von Werkstätten.',
        icon: 'tabler:clock',
      },
      {
        title: 'Full-Service inklusive',
        description: 'Hardware, Software, Wartung und Support — alles in einem Jahresvertrag. Keine versteckten Kosten.',
        icon: 'tabler:package',
      },
    ]}
    image={{
      src: '/images/installation-placeholder.svg',
      alt: 'KONVOI Installation — unter 120 Minuten pro Trailer',
    }}
  />

  {/* End CTA */}
  <CallToAction
    id="consult"
    title={t('homepage.cta_title', locale)}
    subtitle={t('homepage.cta_subtitle', locale)}
    actions={[
      {
        variant: 'primary',
        text: t('cta.book_consult', locale),
        href: 'mailto:info@konvoi.eu?subject=Produktberatung',
      },
    ]}
  />
</PageLayout>
```

**Step 3 — Create installation placeholder SVG** at `public/images/installation-placeholder.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 600 300" style="font-family:sans-serif;background:#f8fafc">
  <rect width="600" height="300" fill="#f8fafc" rx="8"/>
  <text x="300" y="140" text-anchor="middle" font-size="16" fill="#64748b">Installationsvideo</text>
  <text x="300" y="168" text-anchor="middle" font-size="13" fill="#94a3b8">Wird vor Launch hinzugefügt — unter 120 Minuten</text>
  <circle cx="300" cy="80" r="36" fill="none" stroke="#3b82f6" stroke-width="2.5"/>
  <polygon points="290,65 290,95 320,80" fill="#3b82f6"/>
</svg>
```
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && pnpm check:astro 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/pages/produkt.astro` — file exists
    - `grep "120 Minuten" src/pages/produkt.astro` returns a match (PROD-05 install promise)
    - `grep "Erkennung" src/pages/produkt.astro` returns a match (PROD-03 step 1)
    - `grep "Klassifizierung" src/pages/produkt.astro` returns a match (PROD-03 step 2)
    - `grep "Maßnahmen" src/pages/produkt.astro` returns a match (PROD-03 step 3)
    - `grep "Camera Module" src/pages/produkt.astro` returns a match (PROD-04)
    - `grep "Logbook" src/pages/produkt.astro` returns a match (PROD-04)
    - `grep "sensor-positions-placeholder" src/pages/produkt.astro` returns a match (PROD-02)
    - pnpm check:astro exits 0
  </acceptance_criteria>
  <done>src/pages/produkt.astro has all 5 PROD sections with real Konvoi DE copy; pnpm check:astro passes</done>
</task>

<task type="auto">
  <name>Task 2: Build EN product page src/pages/en/product.astro (PROD-01)</name>
  <files>src/pages/en/product.astro</files>
  <read_first>
    - src/pages/produkt.astro (just created — mirror structure with EN copy)
    - src/data/brand/canonical.yaml (en_name and en_description for tier names)
    - src/data/brand/voice.md (British English, approved verbs)
    - src/i18n/translations.ts (product.* EN strings)
  </read_first>
  <action>
Create `src/pages/en/product.astro` mirroring the DE product page structure with EN copy. The `en/` directory should already exist from Phase 3 (src/pages/en/ was created for the EN homepage). Verify it exists before writing.

```astro
---
import PageLayout from '~/layouts/PageLayout.astro';
import Hero from '~/components/widgets/Hero.astro';
import Content from '~/components/widgets/Content.astro';
import Steps from '~/components/widgets/Steps.astro';
import Features2 from '~/components/widgets/Features2.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';
import { t } from '~/i18n/translations';
import brand from '~/data/brand/canonical.yaml';

const locale = 'en';

const metadata = {
  title: 'KONVOI Product — Sensor-Based Security System for Trailers',
  description:
    'The KONVOI system: GPS, shock and motion sensors, AI classification, and an immediate deterrence chain. Installation in under 120 minutes.',
};
---

<PageLayout metadata={metadata}>

  <Hero
    tagline={t('product.hero_tagline', locale)}
    title={t('product.hero_title', locale)}
    subtitle={t('product.hero_subtitle', locale)}
    videoPoster="/images/hero-poster.jpg"
    actions={[
      {
        variant: 'primary',
        text: t('cta.book_consult', locale),
        href: '#consult',
      },
    ]}
  />

  <Content
    id="hardware"
    tagline={t('product.hardware_tagline', locale)}
    title={t('product.hardware_title', locale)}
    subtitle={t('product.hardware_subtitle', locale)}
    items={[
      {
        title: 'GPS + LTE Connectivity',
        description: 'Real-time tracking and data transmission, even in areas with weak signal coverage.',
        icon: 'tabler:map-pin',
      },
      {
        title: 'Shock and Motion Sensors',
        description: 'Detect suspicious patterns — from abnormal door movements to unexpected vibrations.',
        icon: 'tabler:wave-sine',
      },
      {
        title: '7-Day Battery',
        description: 'Self-sufficient. No connection to vehicle electronics required. Operates even when the trailer is uncoupled.',
        icon: 'tabler:battery',
      },
      {
        title: 'Concealed Housing',
        description: 'Compact and discreet mounting. No visible indication of the security system for potential thieves.',
        icon: 'tabler:eye-off',
      },
    ]}
    image={{
      src: '/images/sensor-positions-placeholder.svg',
      alt: 'Sensor positions on trailer — KONVOI system overview',
    }}
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Content>

  <Steps
    id="how-it-works"
    tagline={t('product.steps_tagline', locale)}
    title={t('product.steps_title', locale)}
    items={[
      {
        title: 'Detection',
        description:
          'Shock, motion, and GPS sensors continuously collect data. Anomalies are registered immediately — e.g. abnormal door movements at 2 am at a motorway rest area.',
        icon: 'tabler:radar',
      },
      {
        title: 'Classification',
        description:
          'The KONVOI AI analyses sensor patterns and classifies the event as a threat or false alarm. No human intervention required — the decision takes seconds.',
        icon: 'tabler:brain',
      },
      {
        title: 'Measures',
        description:
          'Upon confirmed threat, KONVOI immediately triggers the deterrence chain: siren, driver notification, emergency services alert. Theft is prevented, not just documented.',
        icon: 'tabler:shield-check',
      },
    ]}
  />

  <Features2
    id="add-ons"
    tagline={t('product.addons_tagline', locale)}
    title={t('product.addons_title', locale)}
    subtitle="Extend the KONVOI Standard system as needed."
    columns={2}
    items={[
      {
        title: brand.pricing.tiers[1].en_name,
        description:
          `${brand.pricing.tiers[1].en_description} — A video clip is automatically stored in the cloud on every classified event. Comprehensive evidence for insurance and law enforcement.`,
        icon: 'tabler:video',
      },
      {
        title: brand.pricing.tiers[2].en_name,
        description:
          `${brand.pricing.tiers[2].en_description} — Ideal for compliance requirements and communication with clients. Every journey transparently documented.`,
        icon: 'tabler:file-description',
      },
    ]}
  >
    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Features2>

  <Content
    id="installation"
    tagline={t('product.install_tagline', locale)}
    title={t('product.install_title', locale)}
    isReversed
    items={[
      {
        title: 'Our own installation team',
        description: 'No third-party contractors. Our team knows the system inside out and guarantees a clean, concealed installation.',
        icon: 'tabler:tool',
      },
      {
        title: brand.install.en_promise,
        description: 'No trailer needs to be out of operation longer than necessary. No dependency on workshop availability.',
        icon: 'tabler:clock',
      },
      {
        title: 'Full service included',
        description: 'Hardware, software, maintenance, and support — all in a single annual contract. No hidden costs.',
        icon: 'tabler:package',
      },
    ]}
    image={{
      src: '/images/installation-placeholder.svg',
      alt: 'KONVOI installation — under 120 minutes per trailer',
    }}
  />

  <CallToAction
    id="consult"
    title={t('homepage.cta_title', locale)}
    subtitle={t('homepage.cta_subtitle', locale)}
    actions={[
      {
        variant: 'primary',
        text: t('cta.book_consult', locale),
        href: 'mailto:info@konvoi.eu?subject=Product+Enquiry',
      },
    ]}
  />
</PageLayout>
```
  </action>
  <verify>
    <automated>cd /c/Users/Rami/Desktop/KONVOI-HOMEPAGE && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/pages/en/product.astro` — file exists
    - `grep "120 minutes" src/pages/en/product.astro` returns a match (PROD-05)
    - `grep "Detection" src/pages/en/product.astro` returns a match (PROD-03 step 1)
    - `grep "Classification" src/pages/en/product.astro` returns a match (PROD-03 step 2)
    - `grep "Measures" src/pages/en/product.astro` returns a match (PROD-03 step 3)
    - `grep "Camera Module" src/pages/en/product.astro` returns a match (PROD-04)
    - `pnpm build` exits 0
    - `test -f dist/produkt/index.html` — DE product page built
    - `test -f dist/en/product/index.html` — EN product page built
  </acceptance_criteria>
  <done>Both product pages build; all 5 PROD sections present in DE and EN; pnpm build exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical.yaml → template | Tier names/descriptions sourced from YAML and rendered via template literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | canonical.yaml brand data injected via template literal | accept | YAML is a static project file committed to git; not user input; Astro renders it at build time as escaped HTML; no runtime eval |
| T-04-03-02 | Tampering | Placeholder SVG files | accept | SVGs are static inline images; no scripts; no external fetch; file is committed by the executor |
| T-04-03-03 | Disclosure | mailto: href on product CTA | accept | email is already public on konvoi.eu; no form data; no PII transmission |
</threat_model>

<verification>
After both tasks:
1. `pnpm build` exits 0
2. `test -f dist/produkt/index.html` passes
3. `test -f dist/en/product/index.html` passes
4. `grep "120 Minuten" dist/produkt/index.html` returns a match
5. `grep "120 minutes" dist/en/product/index.html` returns a match
6. `grep -i "detection\|erkennung" dist/produkt/index.html` returns a match
</verification>

<success_criteria>
- src/pages/produkt.astro has all 5 PROD sections with real DE copy from voice.md + canonical.yaml + current-site-overview.md
- src/pages/en/product.astro mirrors with EN copy
- Sensor positions diagram placeholder SVG exists at public/images/sensor-positions-placeholder.svg
- pnpm build exits 0; both pages appear in dist/
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-marketing-pages/04-03-SUMMARY.md` following the summary template.
</output>
