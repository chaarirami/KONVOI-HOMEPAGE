---
phase: 02-brand-design-system
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/brand/canonical.yaml
  - src/data/brand/voice.md
autonomous: false
requirements: [BRAND-05, BRAND-06]

must_haves:
  truths:
    - "src/data/brand/canonical.yaml exists with Konvoi legal entity, address, phone, emails, and pricing tier structure"
    - "src/data/brand/voice.md exists with approved vs banned verb lists for preventive-vs-reactive positioning"
    - "Both files are readable and well-formed (YAML parses cleanly, Markdown renders cleanly)"
    - "No secrets (API keys, tokens, passwords) appear in either file"
  artifacts:
    - path: "src/data/brand/canonical.yaml"
      provides: "Single source of truth for legal entity, address, phone, contact emails, pricing tier names"
      contains: "KONVOI GmbH"
    - path: "src/data/brand/voice.md"
      provides: "Approved vs banned verb list, tone guidelines, copy transformation examples"
      contains: "preventive"
  key_links:
    - from: "src/data/brand/canonical.yaml"
      to: "src/components (future consumers)"
      via: "direct YAML import in Astro frontmatter"
      pattern: "import.*canonical.yaml"
    - from: "src/data/brand/voice.md"
      to: "copywriters / content authors"
      via: "reference document, not imported at runtime"
      pattern: "Approved verbs"
---

<objective>
Create src/data/brand/ directory with two files: canonical.yaml (single source of truth for Konvoi legal data and pricing tier structure) and voice.md (approved vs banned verbs for the preventive-vs-reactive brand positioning). Both files are sourced from .planning/current-site-overview.md and PROJECT.md, then reviewed by the user.

Purpose: After this plan, Phase 4+ components can import canonical.yaml for legal data, prices, and contact info instead of hard-coding them. voice.md ensures content authors apply consistent brand language across all pages.

Output: src/data/brand/canonical.yaml and src/data/brand/voice.md, both committed and ready for user review.
</objective>

<execution_context>
@C:/Users/RamiChaari/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RamiChaari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-design-system/02-CONTEXT.md
@.planning/current-site-overview.md

<interfaces>
<!-- Source data for canonical.yaml — extracted from current-site-overview.md and CONTEXT.md D-09 -->

Legal entity data (from current-site-overview.md §1):
- Entity: KONVOI GmbH
- Address: Harburger Schlossstraße 6-12, 21079 Hamburg, Germany
- Phone: +49 40 766293660
- Contacts on live site: Heinz Luckhardt (CEO/investors/marketing), Alexander Jagielo (co-founder)
- Emails from REQUIREMENTS.md CONT-01: justus@konvoi.eu (customer advisor), heinz@konvoi.eu (investors/marketing)
- General/applications emails from REQUIREMENTS.md CAREER-02: applications@konvoi.eu; CONT-01: info@konvoi.eu

Pricing tier data (from current-site-overview.md §8 and REQUIREMENTS.md PRICE-02):
- Three tiers: Standard, + Camera Module, + Logbook
- Prices on live site are not publicly listed (quote-based); use placeholder "auf Anfrage" / "on request"
- Format: "ab X EUR / Monat" per UI-SPEC §7 — leave price amounts as "X" pending sales sign-off

Brand voice data (from PROJECT.md and current-site-overview.md §1):
- Core positioning: "The first preventive solution for your fleet"
- Approved: prevent, anticipate, secure, detect, classify, deter, enable
- Banned: respond, react, alert (implies reactive), "respond to alerts", "react after"
- Tone: Professional B2B, German engineering confidence, data-driven

Pricing data note from REQUIREMENTS.md PRICE-03:
- canonical.yaml tier data is shared with ROI calculator (Phase 5)
- Tier slugs must be stable: standard, camera-module, logbook
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/data/brand/canonical.yaml</name>
  <files>src/data/brand/canonical.yaml</files>
  <read_first>
    - .planning/current-site-overview.md (sections 1 and 8 — legal entity and pricing data)
    - .planning/phases/02-brand-design-system/02-CONTEXT.md (D-09 — data source and field list)
    - .planning/REQUIREMENTS.md (CONT-01 for email addresses, PRICE-02 for tier names)
  </read_first>
  <action>
Create the directory `src/data/brand/` and write the file `src/data/brand/canonical.yaml` with the following exact content.

**Note on pricing amounts:** The live site does not publish prices publicly (quote-based). Use `"auf Anfrage"` / `"on request"` as placeholders. The user will fill in actual prices before Phase 5 (pricing pages). The tier slugs (`standard`, `camera-module`, `logbook`) MUST be stable — Phase 5 ROI calculator will reference them by slug (REQUIREMENTS.md PRICE-03).

```yaml
# src/data/brand/canonical.yaml
# Single source of truth for Konvoi legal entity, contact, and pricing data.
# Sourced from: .planning/current-site-overview.md (2026-04-20 snapshot)
# Last updated: Phase 2 Brand & Design System
#
# USAGE: import this file in Astro components via:
#   import brand from '~/data/brand/canonical.yaml';
#
# USER REVIEW REQUIRED: Verify all fields before Phase 3 merges.

legal:
  entity: KONVOI GmbH
  address: "Harburger Schlossstraße 6-12, 21079 Hamburg, Germany"
  address_de: "Harburger Schlossstraße 6-12, 21079 Hamburg, Deutschland"
  phone: "+49 40 766293660"
  website: "https://www.konvoi.eu"

contacts:
  customer_advisor:
    name: Justus Männinghoff
    role_de: Kundenberatung
    role_en: Customer Advisor
    email: justus@konvoi.eu
  marketing:
    name: Heinz Luckhardt
    role_de: "Investoren, Marketing & Bewerber"
    role_en: "Investors, Marketing & Applicants"
    email: heinz@konvoi.eu
  general:
    email: info@konvoi.eu
  applications:
    email: applications@konvoi.eu

pricing:
  currency: EUR
  billing_period_de: Monat
  billing_period_en: month
  tiers:
    - slug: standard
      de_name: "KONVOI Standard"
      en_name: "KONVOI Standard"
      de_price_display: "auf Anfrage"
      en_price_display: "on request"
      de_description: "GPS + LTE, Schock- und Bewegungssensoren, KI-Klassifizierung, Alarmkette"
      en_description: "GPS + LTE, shock and motion sensors, AI classification, alarm chain"
    - slug: camera-module
      de_name: "+ KONVOI Camera Module"
      en_name: "+ KONVOI Camera Module"
      de_price_display: "auf Anfrage"
      en_price_display: "on request"
      de_description: "Cloud-Clips bei Ereignissen"
      en_description: "Cloud clips on security events"
    - slug: logbook
      de_name: "+ KONVOI Logbook"
      en_name: "+ KONVOI Logbook"
      de_price_display: "auf Anfrage"
      en_price_display: "on request"
      de_description: "PDF-Zusammenfassung der Stopps und Anomalien je Fahrt"
      en_description: "PDF summary of stops and anomalies per trip"

funding:
  de_minimis_max_percent: 80
  de_catalog_ref: "Aufwendungen für Maßnahmen zur Vermeidung von Diebstählen"
  de_catalog_section: "1.10"

install:
  max_minutes: 120
  de_promise: "Installation in unter 120 Minuten"
  en_promise: "Installation in under 120 minutes"
```
  </action>
  <verify>
    <automated>cd /c/Users/RamiChaari/Desktop/KONVOI-HOMEPAGE && test -f src/data/brand/canonical.yaml && grep "KONVOI GmbH" src/data/brand/canonical.yaml && grep "justus@konvoi.eu" src/data/brand/canonical.yaml && grep "slug: standard" src/data/brand/canonical.yaml && grep "slug: camera-module" src/data/brand/canonical.yaml && grep "slug: logbook" src/data/brand/canonical.yaml && echo "PASS: canonical.yaml looks correct"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/data/brand/canonical.yaml` exits 0
    - File contains `entity: KONVOI GmbH`
    - File contains `phone: "+49 40 766293660"`
    - File contains `email: justus@konvoi.eu`
    - File contains `email: heinz@konvoi.eu`
    - File contains `email: info@konvoi.eu`
    - File contains `email: applications@konvoi.eu`
    - File contains `slug: standard`
    - File contains `slug: camera-module`
    - File contains `slug: logbook`
    - File does NOT contain any password, API key, or token patterns (grep for `secret|token|password|key:` returns no matches)
    - File is valid YAML (no parse errors when read by a YAML parser — `pnpm build` exercises this via Astro's data handling)
  </acceptance_criteria>
  <done>src/data/brand/canonical.yaml created with legal entity, contact emails, three pricing tier slugs, funding de-minimis ref, and install promise. No secrets present.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/data/brand/voice.md</name>
  <files>src/data/brand/voice.md</files>
  <read_first>
    - .planning/current-site-overview.md (sections 1 and 6 — homepage copy and positioning language)
    - .planning/PROJECT.md (core value and positioning)
    - .planning/phases/02-brand-design-system/02-CONTEXT.md (D-10 — extraction approach)
  </read_first>
  <action>
Create the file `src/data/brand/voice.md` with the following content. This file is a reference document for copywriters and content authors — it is not imported at runtime by components.

```markdown
# Konvoi Brand Voice Guide

> Reference document for copywriters, content authors, and Claude agents generating marketing copy.
> Sourced from: live konvoi.eu copy + PROJECT.md positioning (2026-04-20).
> **User review required** before Phase 4 content pages are written.

---

## Core Positioning

**KONVOI is "the first preventive solution for your fleet."**

This single sentence defines the brand category. Every piece of copy reinforces the preventive (not reactive) distinction.

Tagline: **"SECURITY TECH MADE IN GERMANY"**

Sub-tagline: **"The first preventive solution for your fleet — against cargo & diesel theft, sabotage & driver assaults"**

---

## Approved Verbs (Preventive Language)

Use these verbs to describe what KONVOI does:

| Verb | Context | Example |
|------|---------|---------|
| **prevent** | Primary action | "KONVOI prevents cargo theft before it happens" |
| **anticipate** | Intelligence/AI | "Our AI anticipates threats based on motion patterns" |
| **secure** | Outcome | "Secure your fleet against theft and sabotage" |
| **detect** | Technical capability | "Sensors detect shock, motion, and GPS anomalies" |
| **classify** | AI capability | "AI classifies events as threat vs. non-threat in real time" |
| **deter** | Alarm chain | "Visible deterrence reduces theft risk at rest areas" |
| **enable** | Customer outcome | "Enables your team to focus on operations, not incident response" |
| **protect** | General security | "Protect high-value cargo end-to-end" |

---

## Banned Verbs (Reactive Language — DO NOT USE)

These verbs imply KONVOI reacts after theft has occurred. They undermine the preventive positioning.

| Banned Verb | Why | Use Instead |
|-------------|-----|-------------|
| **respond** | Implies reaction after event | "prevent" / "anticipate" |
| **react** | Implies reactive posture | "detect and classify" |
| **alert** (as primary feature) | Implies the product just sends notifications | "prevents and deters" |
| **notify** (as primary feature) | Same as alert | "classifies and acts" |
| **track** (as primary feature) | Sounds like GPS tracker, not security system | "secures and monitors" |

**Note:** "alert" and "notify" are acceptable when describing the alarm chain (a component of the system), but should never be the lead feature claim.

---

## Tone Guidelines

### B2B, Professional, Data-Driven

- Write for **fleet managers and logistics directors** — people responsible for P&L and compliance
- Use **numbers and anchors** when possible: "€8B/yr cargo theft in Europe (TAPA 2024)", "up to 80% de-minimis reimbursement", "installation in under 120 minutes"
- Confidence without hype — claim capability the product actually delivers
- No superlatives without evidence ("best", "fastest", "most advanced" — avoid unless sourced)

### German Engineering Confidence

- Hamburg origin and TUHH spin-off are credibility signals — use them
- Precision matters: "7-day self-sufficient battery", "≤120 min installation", "AI classification" — not vague claims
- "Made in Germany" — use as trust signal, not cliché. Pair with specific technical claims.

### B2B Direct (Not Consumer Marketing)

- No exclamation points unless quoting customer testimonials
- No "you won't believe" or clickbait phrasing
- Short paragraphs — logistics directors scan, not read
- Use bullet points for feature lists; narrative prose for use-case problems and outcomes

---

## Copy Transformation Examples

### Feature Description

| Avoid | Use Instead |
|-------|-------------|
| "Our system alerts you when theft is detected" | "Our system **detects and prevents** cargo theft — before loss occurs" |
| "Get notified of security events on your trailer" | "AI classifies events in real time and triggers an alarm chain to **deter** threats" |
| "Track your fleet with GPS" | "Secure your fleet with sensor-based prevention: GPS + motion + shock, classified by AI" |

### CTAs (Calls to Action)

| Avoid | Use Instead |
|-------|-------------|
| "Sign up" | "Beratung anfragen" (DE) / "Book a consult" (EN) |
| "Try for free" | Not applicable — Konvoi is B2B direct, quote-based |
| "Learn more" | "Mehr erfahren" — acceptable as secondary CTA only |
| "Get started" | Too SaaS / consumer — use "Beratung anfragen" |

### Problem Framing (Use-Case Pages)

Structure: **Cost anchor → Problem context → Konvoi approach**

Example (cargo theft):
> "Cargo theft costs the European logistics industry **€8 billion per year** (TAPA 2024). A single pharma shipment can represent **€1.2 million** in lost goods. Traditional tracking devices tell you where your trailer was stolen from — after the fact. KONVOI **prevents** theft by classifying threat patterns in real time and triggering a deterrence chain before cargo leaves the trailer."

---

## Localisation Notes

- **German (DE):** Default locale. Formal "Sie" — not informal "du". Use full German legal/regulatory terms when citing subsidies ("de-minimis-Förderung", not just "subsidy").
- **English (EN):** British English preferred for EU market. "organisation" not "organization". "colour" not "color". Exception: "KONVOI" is always all-caps in both locales.
- **KONVOI:** Always written in ALL CAPS. Never "Konvoi" in marketing copy (exception: code variables and file paths use camelCase as needed).

---

## Out of Scope for Brand Voice

- Legal copy (Impressum, Datenschutz) — neutral, factual, regulated language
- Technical documentation (sensor specs, API docs) — precise technical language, no marketing spin
- Internal communications — not governed by this guide
```
  </action>
  <verify>
    <automated>cd /c/Users/RamiChaari/Desktop/KONVOI-HOMEPAGE && test -f src/data/brand/voice.md && grep "preventive" src/data/brand/voice.md && grep "Approved Verbs" src/data/brand/voice.md && grep "Banned Verbs" src/data/brand/voice.md && grep "Beratung anfragen" src/data/brand/voice.md && echo "PASS: voice.md looks correct"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/data/brand/voice.md` exits 0
    - File contains the string `preventive` at least 3 times
    - File contains `## Approved Verbs`
    - File contains `## Banned Verbs`
    - File contains `Beratung anfragen`
    - File contains `SECURITY TECH MADE IN GERMANY`
    - File contains `€8B` or `€8 billion` (cost anchor for cargo theft)
    - File is valid Markdown (no unclosed fences, no broken headers)
  </acceptance_criteria>
  <done>src/data/brand/voice.md created with approved/banned verb lists, tone guidelines, copy transformation examples, CTA guidance, and localisation notes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: User review of canonical.yaml and voice.md</name>
  <what-built>
    - src/data/brand/canonical.yaml — legal entity, address, phone, contact emails (justus, heinz, info, applications), three pricing tier slugs (standard, camera-module, logbook) with placeholder prices, funding de-minimis ref, install promise
    - src/data/brand/voice.md — preventive-vs-reactive positioning, approved/banned verb lists, tone guidelines, CTA guidance, copy transformation examples, localisation notes
  </what-built>
  <how-to-verify>
    1. Open src/data/brand/canonical.yaml and check:
       - Legal address is correct (Harburger Schlossstraße 6-12, 21079 Hamburg)
       - Phone number is correct (+49 40 766293660)
       - All four email addresses are correct (justus, heinz, info, applications)
       - Contact names are correct (Justus Männinghoff, Heinz Luckhardt)
       - Tier slugs (standard, camera-module, logbook) are final — they will be referenced by Phase 5 ROI calculator
       - Funding de-minimis percentage (80%) is accurate
    2. Open src/data/brand/voice.md and check:
       - The approved verbs list matches how your team actually talks about the product
       - The banned verbs list correctly captures language to avoid
       - The CTA labels ("Beratung anfragen" / "Book a consult") are the correct primary CTA phrasing
       - The copy transformation examples reflect the real preventive-vs-reactive distinction
    3. Make any corrections directly to the files — small text edits are fine without re-running the executor
    4. Note any fields that need to be filled in (e.g., actual pricing amounts) — these can be updated before Phase 5
  </how-to-verify>
  <resume-signal>Type "approved" after reviewing both files. Or list specific corrections: "fix contact name to X", "change phone to Y", "add verb Z to approved list", etc.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data file → build output | canonical.yaml is read at build time; rendered into HTML pages |
| canonical.yaml → public web | Contact emails and phone are public (already on live site); no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Disclosure | src/data/brand/canonical.yaml | accept | File contains only publicly available business data (address, phone, emails already on live konvoi.eu). No API keys, tokens, or passwords — enforced by acceptance criteria grep check. |
| T-02-10 | Tampering | canonical.yaml pricing tier slugs | mitigate | Slugs (standard, camera-module, logbook) are documented as stable references for Phase 5 ROI calculator. A comment in the file warns against renaming. Changing slugs without updating Phase 5 code would break the ROI calculator. |
| T-02-11 | Information Disclosure | voice.md reveals brand strategy | accept | Brand strategy documents are internal reference material, not served to the browser. voice.md stays in src/data/brand/ which is not a page route — Astro does not serve files in src/data/ as URLs. Risk: developer reads the file. Accepted for a marketing site. |
</threat_model>

<verification>
After all tasks complete and checkpoint approved:

```bash
# Both files exist
test -f src/data/brand/canonical.yaml && echo "PASS: canonical.yaml" || echo "FAIL"
test -f src/data/brand/voice.md && echo "PASS: voice.md" || echo "FAIL"

# Required fields present
grep "KONVOI GmbH" src/data/brand/canonical.yaml
grep "justus@konvoi.eu" src/data/brand/canonical.yaml
grep "slug: standard" src/data/brand/canonical.yaml

# No secrets
grep -iE "secret|token|password|api_key" src/data/brand/canonical.yaml && echo "FAIL: secrets found" || echo "PASS: no secrets"

# Voice guide has key sections
grep "Approved Verbs" src/data/brand/voice.md
grep "Banned Verbs" src/data/brand/voice.md

# Build still passes
pnpm build
```
</verification>

<success_criteria>
1. `src/data/brand/canonical.yaml` exists and contains KONVOI GmbH, all four email addresses, three tier slugs
2. `src/data/brand/voice.md` exists and contains approved/banned verb sections and CTA guidance
3. No secrets in canonical.yaml (`grep -iE "secret|token|password|api_key"` returns no matches)
4. `pnpm build` exits 0
5. User has reviewed and approved both files at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-design-system/02-03-SUMMARY.md` using the summary template.
</output>
