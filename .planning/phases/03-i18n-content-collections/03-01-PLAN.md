---
phase: 03-i18n-content-collections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.ts
  - src/i18n/routeMap.ts
  - src/i18n/translations.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "Astro resolves DE routes at / and EN routes at /en/ with no dynamic [locale] segments"
    - "Missing EN pages return 404, never silently serve DE content"
    - "getLocalePath('/anwendungen/ladungsdiebstahl/', 'en') returns '/en/use-cases/cargo-theft/'"
    - "getLocalePath('/en/use-cases/cargo-theft/', 'de') returns '/anwendungen/ladungsdiebstahl/'"
    - "t('nav.home', 'de') returns 'Startseite', t('nav.home', 'en') returns 'Home'"
  artifacts:
    - path: "astro.config.ts"
      provides: "i18n routing config"
      contains: "defaultLocale: 'de'"
    - path: "src/i18n/routeMap.ts"
      provides: "bidirectional route map + getLocalePath helper"
      exports: ["routeMap", "getLocalePath"]
    - path: "src/i18n/translations.ts"
      provides: "UI string translations + t() helper"
      exports: ["translations", "t"]
  key_links:
    - from: "astro.config.ts"
      to: "Astro routing"
      via: "i18n config block"
      pattern: "defaultLocale.*de"
    - from: "src/components/widgets/LanguageSwitcher.astro"
      to: "src/i18n/routeMap.ts"
      via: "getLocalePath import"
      pattern: "import.*getLocalePath.*routeMap"
---

<objective>
Wire Astro's native i18n routing configuration and create the two utility modules that every locale-aware component will depend on: a static bidirectional route map and a UI string translation helper.

Purpose: Establishes the routing foundation (I18N-01/02) so DE serves at `/` and EN at `/en/`, and provides getLocalePath() (I18N-03) so the language switcher (Plan 04) can navigate between locale equivalents without hardcoded paths.

Output: Three files — `astro.config.ts` with i18n block, `src/i18n/routeMap.ts`, `src/i18n/translations.ts`.
</objective>

<execution_context>
@/c/Users/RamiChaari/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/RamiChaari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-i18n-content-collections/03-CONTEXT.md

<interfaces>
<!-- Key signatures and types the executor needs. Extracted from codebase. -->

From astro.config.ts (current — lines 18-76):
```typescript
export default defineConfig({
  output: 'static',
  integrations: [ sitemap(), mdx(), icon({...}), compress({...}), astrowind({...}) ],
  image: { domains: ['cdn.pixabay.com', 'images.unsplash.com', 'plus.unsplash.com'] },
  markdown: { remarkPlugins: [...], rehypePlugins: [...] },
  vite: { plugins: [tailwindcss()], resolve: { alias: { '~': path.resolve(__dirname, './src') } } },
});
```
Insert `i18n: { ... }` after `output: 'static'` and before `integrations: [`.

From src/utils/permalinks.ts (export pattern to follow):
```typescript
export const getPermalink = (slug = '', type = 'page'): string => { ... };
export const getHomePermalink = (): string => getPermalink('/');
```

Route slugs required in routeMap (from REQUIREMENTS.md):
- UC-01: 7 use-case pairs (e.g. /anwendungen/ladungsdiebstahl/ ↔ /en/use-cases/cargo-theft/)
- VERT-01: 4 industry pairs
- BLOG-01: /aktuelles/ ↔ /en/news/
- CASE-01: /fallstudien/ ↔ /en/case-studies/
- Top-level pages: home, about, contact, pricing, product, roi, funding, karriere, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n config block to astro.config.ts</name>
  <files>astro.config.ts</files>
  <read_first>
    - astro.config.ts — read current structure to insert i18n block at the right position (after `output: 'static'`, before `integrations`)
  </read_first>
  <action>
Insert the following `i18n` block into `astro.config.ts` immediately after `output: 'static',` and before the `integrations: [` line:

```typescript
  i18n: {
    defaultLocale: 'de',
    locales: ['de', 'en'],
    prefixDefaultLocale: false,
    fallback: undefined, // No silent fallback — missing EN pages must return 404 (per D-02 / I18N-01)
  },
```

Do NOT change any other part of the file. The `astrowind` integration and all existing config keys stay untouched.

Per D-01: `prefixDefaultLocale: false` means DE routes have no `/de/` prefix — they serve at `/`.
Per D-02: `fallback: undefined` is critical — Astro's default is to silently fall back to DE; setting it to `undefined` disables that behavior.
  </action>
  <verify>
    <automated>cd /c/Users/RamiChaari/Desktop/KONVOI-HOMEPAGE && grep -n "defaultLocale.*de" astro.config.ts && grep -n "prefixDefaultLocale.*false" astro.config.ts && grep -n "fallback.*undefined" astro.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.ts` contains `defaultLocale: 'de'`
    - `astro.config.ts` contains `locales: ['de', 'en']`
    - `astro.config.ts` contains `prefixDefaultLocale: false`
    - `astro.config.ts` contains `fallback: undefined`
    - `pnpm check:astro` exits 0 (no TypeScript errors introduced)
  </acceptance_criteria>
  <done>Astro i18n config is present and type-checks cleanly. Astro.currentLocale will now be available in all components.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/i18n/routeMap.ts and src/i18n/translations.ts</name>
  <files>src/i18n/routeMap.ts, src/i18n/translations.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md — lines 67-69 (UC-01 slug pairs), line 76 (VERT-01), line 131 (BLOG-01), line 83 (CASE-01) — extract exact DE and EN slug strings
    - .planning/phases/03-i18n-content-collections/03-CONTEXT.md — D-04, D-05, D-13, D-14 for exact structure
    - src/utils/permalinks.ts — lines 1-10 for import/export convention to replicate
  </read_first>
  <action>
**Create `src/i18n/routeMap.ts`** with this exact content (per D-04 / D-05 / I18N-03):

```typescript
// src/i18n/routeMap.ts
// Static bidirectional route map for language switcher navigation.
// Phase 3 stubs all known routes from REQUIREMENTS.md.
// Content phases (4-7) update entries as pages are created.
// Per D-04: keyed by canonical route key, values are locale-specific paths.

export const routeMap: Record<string, { de: string; en: string }> = {
  // Top-level pages
  home:       { de: '/',           en: '/en/' },
  about:      { de: '/about/',     en: '/en/about/' },
  contact:    { de: '/kontakt/',   en: '/en/contact/' },
  product:    { de: '/produkt/',   en: '/en/product/' },
  pricing:    { de: '/preise/',    en: '/en/pricing/' },
  roi:        { de: '/roi/',       en: '/en/roi/' },
  funding:    { de: '/foerderung/', en: '/en/funding/' },
  careers:    { de: '/karriere/',  en: '/en/careers/' },
  news:       { de: '/aktuelles/', en: '/en/news/' },
  'case-studies': { de: '/fallstudien/', en: '/en/case-studies/' },
  thanks:     { de: '/danke/',     en: '/en/thanks/' },

  // Use-case pages (UC-01)
  'use-cases/cargo-theft':             { de: '/anwendungen/ladungsdiebstahl/',       en: '/en/use-cases/cargo-theft/' },
  'use-cases/diesel-theft':            { de: '/anwendungen/dieseldiebstahl/',        en: '/en/use-cases/diesel-theft/' },
  'use-cases/equipment-theft':         { de: '/anwendungen/equipmentdiebstahl/',     en: '/en/use-cases/equipment-theft/' },
  'use-cases/operations-transparency': { de: '/anwendungen/transparenz-der-operationen/', en: '/en/use-cases/operations-transparency/' },
  'use-cases/trailer-damage':          { de: '/anwendungen/trailerschaeden/',        en: '/en/use-cases/trailer-damage/' },
  'use-cases/driver-assaults':         { de: '/anwendungen/fahrerangriffe/',         en: '/en/use-cases/driver-assaults/' },
  'use-cases/stationary-time':         { de: '/anwendungen/standzeit-optimierung/',  en: '/en/use-cases/stationary-time-optimization/' },

  // Industry verticals (VERT-01)
  'industries/high-value':   { de: '/branchen/hochwertige-gueter/', en: '/en/industries/high-value/' },
  'industries/cooling':      { de: '/branchen/kuehlgut/',           en: '/en/industries/cooling/' },
  'industries/intermodal':   { de: '/branchen/intermodal/',         en: '/en/industries/intermodal/' },
  'industries/other':        { de: '/branchen/sonstige/',           en: '/en/industries/other/' },
};

/**
 * Given the current URL pathname and a target locale, return the equivalent
 * path in that locale. Returns null if the current path is not in the map.
 *
 * Per D-04: used by LanguageSwitcher.astro to compute navigation targets.
 * Per D-07: never auto-redirects — caller decides what to do with the result.
 */
export function getLocalePath(
  currentPath: string,
  targetLocale: 'de' | 'en'
): string | null {
  // Normalise trailing slash for comparison
  const normalised = currentPath.endsWith('/') ? currentPath : `${currentPath}/`;
  for (const paths of Object.values(routeMap)) {
    const dePath = paths.de.endsWith('/') ? paths.de : `${paths.de}/`;
    const enPath = paths.en.endsWith('/') ? paths.en : `${paths.en}/`;
    if (dePath === normalised || enPath === normalised) {
      return paths[targetLocale];
    }
  }
  return null;
}
```

**Create `src/i18n/translations.ts`** with this exact content (per D-13 / D-14):

```typescript
// src/i18n/translations.ts
// Static UI string translations for nav labels, CTAs, footer, and form text.
// Per D-13: exported as Record<locale, Record<dot.notation.key, string>>.
// Per D-14: flat dot-notation keys (no nesting). Helper t() exported below.

type Locale = 'de' | 'en';

export const translations: Record<Locale, Record<string, string>> = {
  de: {
    // Navigation
    'nav.home':        'Startseite',
    'nav.product':     'Produkt',
    'nav.use_cases':   'Anwendungen',
    'nav.industries':  'Branchen',
    'nav.case_studies':'Fallstudien',
    'nav.news':        'Aktuelles',
    'nav.pricing':     'Preise',
    'nav.careers':     'Karriere',
    'nav.contact':     'Kontakt',
    'nav.about':       'Über uns',
    // CTAs
    'cta.book_consult':      'Beratung anfragen',
    'cta.learn_more':        'Mehr erfahren',
    'cta.view_case_studies': 'Fallstudien ansehen',
    'cta.calculate_roi':     'ROI berechnen',
    // Footer
    'footer.copyright':   '© Konvoi GmbH',
    'footer.legal':       'Impressum',
    'footer.privacy':     'Datenschutz',
    // Language switcher
    'lang.de': 'DE',
    'lang.en': 'EN',
    'lang.switch_to_de': 'Auf Deutsch wechseln',
    'lang.switch_to_en': 'Switch to English',
    // Forms
    'form.required':     'Pflichtfeld',
    'form.submit':       'Absenden',
    'form.dsgvo_consent':'Ich stimme der Datenschutzerklärung zu',
    // Misc
    'misc.loading':  'Laden...',
    'misc.back':     'Zurück',
  },
  en: {
    // Navigation
    'nav.home':        'Home',
    'nav.product':     'Product',
    'nav.use_cases':   'Use Cases',
    'nav.industries':  'Industries',
    'nav.case_studies':'Case Studies',
    'nav.news':        'News',
    'nav.pricing':     'Pricing',
    'nav.careers':     'Careers',
    'nav.contact':     'Contact',
    'nav.about':       'About Us',
    // CTAs
    'cta.book_consult':      'Book a consult',
    'cta.learn_more':        'Learn more',
    'cta.view_case_studies': 'View case studies',
    'cta.calculate_roi':     'Calculate ROI',
    // Footer
    'footer.copyright':   '© Konvoi GmbH',
    'footer.legal':       'Legal Notice',
    'footer.privacy':     'Privacy Policy',
    // Language switcher
    'lang.de': 'DE',
    'lang.en': 'EN',
    'lang.switch_to_de': 'Auf Deutsch wechseln',
    'lang.switch_to_en': 'Switch to English',
    // Forms
    'form.required':     'Required field',
    'form.submit':       'Submit',
    'form.dsgvo_consent':'I agree to the privacy policy',
    // Misc
    'misc.loading':  'Loading...',
    'misc.back':     'Back',
  },
};

/**
 * Look up a translation key for a given locale.
 * Falls back to the key itself if not found (never throws).
 * Per D-14: use dot-notation keys, e.g. t('nav.home', 'de') → 'Startseite'
 */
export function t(key: string, locale: string): string {
  const loc = (locale === 'en' ? 'en' : 'de') as Locale;
  return translations[loc]?.[key] ?? key;
}
```
  </action>
  <verify>
    <automated>cd /c/Users/RamiChaari/Desktop/KONVOI-HOMEPAGE && grep -n "export const routeMap" src/i18n/routeMap.ts && grep -n "export function getLocalePath" src/i18n/routeMap.ts && grep -n "export const translations" src/i18n/translations.ts && grep -n "export function t" src/i18n/translations.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/i18n/routeMap.ts` exists and exports `routeMap` and `getLocalePath`
    - `routeMap` contains exactly 7 use-case entries matching UC-01 slug pairs (grep: `use-cases/cargo-theft`, `use-cases/diesel-theft`, `use-cases/equipment-theft`, `use-cases/operations-transparency`, `use-cases/trailer-damage`, `use-cases/driver-assaults`, `use-cases/stationary-time`)
    - `routeMap` contains 4 industry entries (grep: `industries/high-value`, `industries/cooling`, `industries/intermodal`, `industries/other`)
    - `getLocalePath` function signature: `(currentPath: string, targetLocale: 'de' | 'en'): string | null`
    - `src/i18n/translations.ts` exists and exports `translations` and `t`
    - `translations` has both `de` and `en` top-level keys
    - `translations.de['nav.home']` equals `'Startseite'`
    - `translations.en['cta.book_consult']` equals `'Book a consult'`
    - `pnpm check:astro` exits 0 after both files are created
  </acceptance_criteria>
  <done>routeMap.ts maps all known DE/EN slug pairs from REQUIREMENTS.md. translations.ts covers all UI strings needed through Phase 4. Both export their helpers and type-check cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → Astro routing | Locale is derived from file-tree position, not user input — no injection surface |
| routeMap lookup → navigation | getLocalePath returns a pre-defined string from a static object; no dynamic construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | i18n routing (fallback config) | mitigate | `fallback: undefined` explicitly set; if omitted, Astro silently serves DE for missing EN paths, potentially hiding 404s. Verified in Task 1 acceptance criteria. |
| T-03-01-02 | Information Disclosure | getLocalePath return value | accept | Returns only pre-defined strings from static map or null; no user-controlled data reflected. Low risk. |
| T-03-01-03 | Tampering | routeMap.ts (static file) | accept | File is version-controlled; no runtime mutation. Any change requires a PR and CI gate. |
</threat_model>

<verification>
After both tasks complete:
1. `grep -n "defaultLocale.*de" astro.config.ts` — must match
2. `grep -c "use-cases/" src/i18n/routeMap.ts` — must return 7
3. `grep -c "industries/" src/i18n/routeMap.ts` — must return 4
4. `grep "export function getLocalePath" src/i18n/routeMap.ts` — must match
5. `grep "export function t" src/i18n/translations.ts` — must match
6. `pnpm check:astro` — exits 0
</verification>

<success_criteria>
- `astro.config.ts` has i18n block with `defaultLocale: 'de'`, `locales: ['de', 'en']`, `prefixDefaultLocale: false`, `fallback: undefined`
- `src/i18n/routeMap.ts` maps all 7 use-case pairs + 4 industry pairs + top-level routes
- `src/i18n/translations.ts` covers nav, CTA, footer, form keys in both locales
- `pnpm check:astro` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-i18n-content-collections/03-01-SUMMARY.md`
</output>
