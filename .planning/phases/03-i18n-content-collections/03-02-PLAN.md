---
phase: 03-i18n-content-collections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/content/post/de/.gitkeep
  - src/content/post/en/.gitkeep
  - src/content/caseStudy/de/.gitkeep
  - src/content/caseStudy/en/.gitkeep
  - src/content/useCase/de/.gitkeep
  - src/content/useCase/en/.gitkeep
  - src/content/industry/de/.gitkeep
  - src/content/industry/en/.gitkeep
  - src/content/job/de/.gitkeep
  - src/content/job/en/.gitkeep
  - src/content/event/.gitkeep
  - src/content/team/.gitkeep
  - src/content/README.md
autonomous: true
requirements:
  - I18N-05
  - I18N-06
  - I18N-07

must_haves:
  truths:
    - "All 7 content collections are registered in src/content.config.ts"
    - "Long-form collections (post, caseStudy, useCase, industry, job) accept locale/translationKey/canonicalKey fields"
    - "Short-form collections (event, team) use {de, en} sibling object fields for bilingual content"
    - "Directory scaffold exists with de/ and en/ subdirs for all 5 long-form collections"
    - "pnpm build exits 0 after directory scaffold is in place"
  artifacts:
    - path: "src/content.config.ts"
      provides: "7 collection definitions with locale-aware Zod schemas"
      contains: "caseStudy"
    - path: "src/content/post/de/.gitkeep"
      provides: "Long-form collection DE directory"
    - path: "src/content/team/.gitkeep"
      provides: "Short-form collection directory"
    - path: "src/content/README.md"
      provides: "Directory structure documentation for content authors"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/post/de/"
      via: "glob loader base path"
      pattern: "base.*src/content/post"
    - from: "src/content.config.ts"
      to: "src/content/team/"
      via: "glob loader base path"
      pattern: "base.*src/content/team"
---

<objective>
Expand `src/content.config.ts` from one legacy collection to all 7 locale-aware collections with Zod schemas, then scaffold the content directory tree so Astro's glob loaders and the parity check script have a valid base to work from.

Purpose: Satisfies I18N-05 (7 collections registered), I18N-06 (long-form locale schema), I18N-07 (short-form bilingual sibling fields). Creates the directory structure that content authors will populate in Phases 4-6.

Output: Updated `src/content.config.ts` with 7 `defineCollection()` calls, plus `src/content/` directory tree with `.gitkeep` markers for all de/en subdirectories.
</objective>

<execution_context>
@/c/Users/RamiChaari/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/RamiChaari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-i18n-content-collections/03-CONTEXT.md
@.planning/phases/03-i18n-content-collections/03-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/content.config.ts (current — full file):
```typescript
import { defineCollection } from 'astro:content';
import { glob } from 'astro/loaders';
import { z } from 'astro/zod';

const metadataDefinition = () => z.object({ ... }).optional();  // lines 5-48, keep verbatim

const postCollection = defineCollection({
  loader: glob({ pattern: ['*.md', '*.mdx'], base: 'src/data/post' }),
  schema: z.object({ publishDate, updateDate, draft, title, excerpt, image, category, tags, author, metadata }),
});

export const collections = { post: postCollection };
```

Key change: base path moves from `src/data/post` → `src/content/post` (per D-11).
Pattern for `**/*.{md,mdx}` glob covering de/ and en/ subdirectories (per D-09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite src/content.config.ts with all 7 collections</name>
  <files>src/content.config.ts</files>
  <read_first>
    - src/content.config.ts — read the full file to preserve metadataDefinition() verbatim and understand current schema fields
    - .planning/phases/03-i18n-content-collections/03-CONTEXT.md — D-08 through D-12 for exact schema requirements
    - .planning/phases/03-i18n-content-collections/03-PATTERNS.md — "src/content.config.ts" section for long-form and short-form schema patterns
  </read_first>
  <action>
Replace the entire contents of `src/content.config.ts` with the following. The `metadataDefinition()` function is preserved verbatim from the current file (lines 5-48). The export changes from `{ post }` to all 7 collections.

Per D-11: base paths change from `src/data/post` to `src/content/{collection}`.
Per D-09: long-form collections use glob `'**/*.{md,mdx}'` to match `de/` and `en/` subdirectories.
Per D-10: short-form collections use `{de, en}` sibling Zod objects for bilingual fields.
Per D-12: `metadataDefinition()` is reused across all collections that need SEO metadata.

```typescript
import { defineCollection } from 'astro:content';
import { glob } from 'astro/loaders';
import { z } from 'astro/zod';

// ---------------------------------------------------------------------------
// Shared Zod helpers
// ---------------------------------------------------------------------------

const metadataDefinition = () =>
  z
    .object({
      title: z.string().optional(),
      ignoreTitleTemplate: z.boolean().optional(),

      canonical: z.url().optional(),

      robots: z
        .object({
          index: z.boolean().optional(),
          follow: z.boolean().optional(),
        })
        .optional(),

      description: z.string().optional(),

      openGraph: z
        .object({
          url: z.string().optional(),
          siteName: z.string().optional(),
          images: z
            .array(
              z.object({
                url: z.string(),
                width: z.number().optional(),
                height: z.number().optional(),
              })
            )
            .optional(),
          locale: z.string().optional(),
          type: z.string().optional(),
        })
        .optional(),

      twitter: z
        .object({
          handle: z.string().optional(),
          site: z.string().optional(),
          cardType: z.string().optional(),
        })
        .optional(),
    })
    .optional();

// ---------------------------------------------------------------------------
// Long-form collections (I18N-06)
// Each entry lives in src/content/{collection}/de/ or en/
// Required fields: locale (enum), translationKey, canonicalKey
// ---------------------------------------------------------------------------

// blog posts (migrated from Jimdo in Phase 6)
const postCollection = defineCollection({
  loader: glob({ pattern: '**/*.{md,mdx}', base: 'src/content/post' }),
  schema: z.object({
    locale: z.enum(['de', 'en']),
    translationKey: z.string(),
    canonicalKey: z.string(),

    publishDate: z.date().optional(),
    updateDate: z.date().optional(),
    draft: z.boolean().optional(),

    title: z.string(),
    excerpt: z.string().optional(),
    image: z.string().optional(),

    category: z.string().optional(),
    tags: z.array(z.string()).optional(),
    author: z.string().optional(),

    metadata: metadataDefinition(),
  }),
});

// customer case studies (Phase 6 — Schumacher, JJX, Greilmeier)
const caseStudyCollection = defineCollection({
  loader: glob({ pattern: '**/*.{md,mdx}', base: 'src/content/caseStudy' }),
  schema: z.object({
    locale: z.enum(['de', 'en']),
    translationKey: z.string(),
    canonicalKey: z.string(),

    title: z.string(),
    customer: z.string(),
    vertical: z.string(),
    problem: z.string(),
    approach: z.string(),
    outcome: z.string(),
    quote: z.string().optional(),
    quoteAttribution: z.string().optional(),
    logo: z.string().optional(),
    publishDate: z.date().optional(),

    metadata: metadataDefinition(),
  }),
});

// product use-case pages (Phase 4 — 7 use cases)
const useCaseCollection = defineCollection({
  loader: glob({ pattern: '**/*.{md,mdx}', base: 'src/content/useCase' }),
  schema: z.object({
    locale: z.enum(['de', 'en']),
    translationKey: z.string(),
    canonicalKey: z.string(),

    title: z.string(),
    slug: z.string(),
    problemStatement: z.string().optional(),
    costAnchor: z.string().optional(),
    konvoiApproach: z.string().optional(),
    relatedIndustries: z.array(z.string()).optional(),
    publishDate: z.date().optional(),

    metadata: metadataDefinition(),
  }),
});

// industry vertical landing pages (Phase 4 — 4 verticals)
const industryCollection = defineCollection({
  loader: glob({ pattern: '**/*.{md,mdx}', base: 'src/content/industry' }),
  schema: z.object({
    locale: z.enum(['de', 'en']),
    translationKey: z.string(),
    canonicalKey: z.string(),

    title: z.string(),
    slug: z.string(),
    riskProfile: z.string().optional(),
    relatedUseCases: z.array(z.string()).optional(),
    publishDate: z.date().optional(),

    metadata: metadataDefinition(),
  }),
});

// job listings (Phase 6 — 8 open roles)
const jobCollection = defineCollection({
  loader: glob({ pattern: '**/*.{md,mdx}', base: 'src/content/job' }),
  schema: z.object({
    locale: z.enum(['de', 'en']),
    translationKey: z.string(),
    canonicalKey: z.string(),

    title: z.string(),
    department: z.string().optional(),
    type: z.enum(['fulltime', 'internship', 'initiative']).optional(),
    applyEmail: z.string().optional(),
    publishDate: z.date().optional(),
    active: z.boolean().default(true),

    metadata: metadataDefinition(),
  }),
});

// ---------------------------------------------------------------------------
// Short-form collections (I18N-07)
// Each entry is a single file with {de, en} sibling fields for bilingual content.
// Shared metadata (photo, dates) uses singular fields (not bilingual).
// ---------------------------------------------------------------------------

// events (Phase 6 — upcoming events list on contact page)
const eventCollection = defineCollection({
  loader: glob({ pattern: '**/*.md', base: 'src/content/event' }),
  schema: z.object({
    name: z.object({
      de: z.string(),
      en: z.string(),
    }),
    description: z
      .object({
        de: z.string(),
        en: z.string(),
      })
      .optional(),
    // Shared singular fields (same for both locales)
    startDate: z.date(),
    endDate: z.date().optional(),
    location: z.string().optional(),
    url: z.string().optional(),
  }),
});

// team members (Phase 6 — 9 members)
const teamCollection = defineCollection({
  loader: glob({ pattern: '**/*.md', base: 'src/content/team' }),
  schema: z.object({
    name: z.object({
      de: z.string(),
      en: z.string(),
    }),
    title: z.object({
      de: z.string(),
      en: z.string(),
    }),
    bio: z.object({
      de: z.string(),
      en: z.string(),
    }),
    // Shared singular fields
    photo: z.string(),
    email: z.string().optional(),
    phone: z.string().optional(),
    order: z.number().optional(),
  }),
});

// ---------------------------------------------------------------------------
// Export (I18N-05)
// ---------------------------------------------------------------------------

export const collections = {
  post: postCollection,
  caseStudy: caseStudyCollection,
  useCase: useCaseCollection,
  industry: industryCollection,
  job: jobCollection,
  event: eventCollection,
  team: teamCollection,
};
```
  </action>
  <verify>
    <automated>cd /c/Users/RamiChaari/Desktop/KONVOI-HOMEPAGE && grep -c "defineCollection" src/content.config.ts && grep "export const collections" src/content.config.ts && grep -c "z.enum\(\['de', 'en'\]\)" src/content.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content.config.ts` contains exactly 7 `defineCollection(` calls (grep count = 7)
    - `export const collections` includes all 7 keys: `post`, `caseStudy`, `useCase`, `industry`, `job`, `event`, `team`
    - `z.enum(['de', 'en'])` appears exactly 5 times (once per long-form collection's `locale` field)
    - `metadataDefinition()` is called in all 5 long-form collection schemas
    - `src/content/post` glob base changed from `src/data/post` to `src/content/post`
    - `pnpm check:astro` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>All 7 collections are defined with locale-aware schemas. Long-form collections use locale enum + translationKey + canonicalKey. Short-form collections use {de, en} sibling fields for bilingual content and singular fields for shared metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold src/content/ directory tree and write README</name>
  <files>
    src/content/post/de/.gitkeep,
    src/content/post/en/.gitkeep,
    src/content/caseStudy/de/.gitkeep,
    src/content/caseStudy/en/.gitkeep,
    src/content/useCase/de/.gitkeep,
    src/content/useCase/en/.gitkeep,
    src/content/industry/de/.gitkeep,
    src/content/industry/en/.gitkeep,
    src/content/job/de/.gitkeep,
    src/content/job/en/.gitkeep,
    src/content/event/.gitkeep,
    src/content/team/.gitkeep,
    src/content/README.md
  </files>
  <read_first>
    - .planning/phases/03-i18n-content-collections/03-RESEARCH.md — "Pitfall 2" section (lines 449-454) about empty directories and directory structure
    - .planning/phases/03-i18n-content-collections/03-CONTEXT.md — D-11 for the directory layout rationale
  </read_first>
  <action>
Create the following empty `.gitkeep` files to establish the directory scaffold. Git does not track empty directories; `.gitkeep` files ensure the structure is committed.

**Long-form collections** (5 collections × 2 locales = 10 directories):
- `src/content/post/de/.gitkeep` — empty file
- `src/content/post/en/.gitkeep` — empty file
- `src/content/caseStudy/de/.gitkeep` — empty file
- `src/content/caseStudy/en/.gitkeep` — empty file
- `src/content/useCase/de/.gitkeep` — empty file
- `src/content/useCase/en/.gitkeep` — empty file
- `src/content/industry/de/.gitkeep` — empty file
- `src/content/industry/en/.gitkeep` — empty file
- `src/content/job/de/.gitkeep` — empty file
- `src/content/job/en/.gitkeep` — empty file

**Short-form collections** (2 collections, single directory each):
- `src/content/event/.gitkeep` — empty file
- `src/content/team/.gitkeep` — empty file

**Create `src/content/README.md`** with this content to document the structure for content authors:

```markdown
# Content Collections

This directory contains all structured content for the Konvoi site.
See `src/content.config.ts` for the full Zod schema for each collection.

## Directory Structure

### Long-form collections (bilingual — separate files per locale)

Each long-form entry has TWO files: one in `de/` and one in `en/`.
Both files must share the same `translationKey` frontmatter value.
The CI parity check (`scripts/check-translation-parity.ts`) enforces this.

```
src/content/
├── post/           # Blog posts (Phase 6)
│   ├── de/
│   └── en/
├── caseStudy/      # Customer case studies (Phase 6)
│   ├── de/
│   └── en/
├── useCase/        # Product use-case pages (Phase 4)
│   ├── de/
│   └── en/
├── industry/       # Industry vertical pages (Phase 4)
│   ├── de/
│   └── en/
└── job/            # Job listings (Phase 6)
    ├── de/
    └── en/
```

### Short-form collections (bilingual — single file with {de, en} sibling fields)

Each short-form entry is ONE file. Bilingual fields are nested objects.
No parity check needed — both locales are always in the same file.

```
src/content/
├── event/          # Upcoming events (Phase 6)
└── team/           # Team members (Phase 6)
```

## Required Frontmatter

### Long-form entries (post, caseStudy, useCase, industry, job)

```yaml
---
locale: 'de'            # or 'en' — must match the directory
translationKey: 'my-slug'  # same value in both de/ and en/ files
canonicalKey: 'my-slug'    # slug without locale prefix
title: 'My Title'
---
```

### Short-form entries (event, team)

```yaml
---
name:
  de: 'Deutsch'
  en: 'English'
bio:
  de: 'Deutsch bio'
  en: 'English bio'
photo: '/images/team/name.jpg'
---
```
```
  </action>
  <verify>
    <automated>cd /c/Users/RamiChaari/Desktop/KONVOI-HOMEPAGE && ls src/content/post/de/.gitkeep && ls src/content/post/en/.gitkeep && ls src/content/team/.gitkeep && ls src/content/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/post/de/.gitkeep` exists
    - `src/content/post/en/.gitkeep` exists
    - `src/content/caseStudy/de/.gitkeep` exists
    - `src/content/caseStudy/en/.gitkeep` exists
    - `src/content/useCase/de/.gitkeep` exists
    - `src/content/useCase/en/.gitkeep` exists
    - `src/content/industry/de/.gitkeep` exists
    - `src/content/industry/en/.gitkeep` exists
    - `src/content/job/de/.gitkeep` exists
    - `src/content/job/en/.gitkeep` exists
    - `src/content/event/.gitkeep` exists
    - `src/content/team/.gitkeep` exists
    - `src/content/README.md` exists and contains `translationKey`
    - `pnpm build` exits 0 (glob loaders find their base directories without errors)
  </acceptance_criteria>
  <done>All 12 content directories are scaffolded with .gitkeep markers. README documents the authoring conventions for content authors in Phases 4-6. pnpm build passes with empty but valid collection directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content files → Zod schema | Frontmatter is parsed and validated by Zod at build time — untrusted content authors cannot bypass schema |
| Build output → rendered pages | Content is markdown; rendered to static HTML at build time — no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Content frontmatter fields | mitigate | Zod schema enforces `locale: z.enum(['de', 'en'])` — typos fail the build. `translationKey: z.string()` and `canonicalKey: z.string()` are required. Build fails on schema violation before any content reaches production. |
| T-03-02-02 | Information Disclosure | team collection (phone, email fields) | mitigate | Fields are `z.string().optional()` — only populated in Phase 6 when content authors explicitly add them. No PII auto-collected from directory structure. Future Phase 6 plan must review what contact data is surfaced in static HTML. |
| T-03-02-03 | Denial of Service | Glob loader scanning empty dirs | accept | Empty directories with `.gitkeep` markers are benign for glob loaders — they return 0 entries with no error. Tested against Astro 6 behavior. |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "defineCollection" src/content.config.ts` — must return 7
2. `grep "caseStudy\|useCase\|industry\|job\|event\|team" src/content.config.ts | wc -l` — must be ≥ 12 (multiple references per collection)
3. `ls src/content/post/de/.gitkeep src/content/post/en/.gitkeep src/content/team/.gitkeep` — all exist
4. `pnpm check:astro` — exits 0
5. `pnpm build` — exits 0 (glob loaders accept empty directories)
</verification>

<success_criteria>
- All 7 collections registered in `src/content.config.ts` with correct Zod schemas
- Long-form collections enforce `locale`, `translationKey`, `canonicalKey`
- Short-form collections enforce `{de, en}` sibling object fields for bilingual content
- Directory tree created with `.gitkeep` markers for all 10 long-form locale dirs + 2 short-form dirs
- `src/content/README.md` documents the authoring conventions
- `pnpm build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-i18n-content-collections/03-02-SUMMARY.md`
</output>
